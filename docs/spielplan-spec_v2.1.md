# Spielplan — App Specification v2.1

*The app is named **Spielplan** (owner, 2026-08-29). Earlier drafts and the design prototype carry the working title "Media Graph".*

**Status:** implementation spec for the new standalone app (new git repo, from scratch). **v2.1 (2026-08-29)** folds in the UI-prototype review (`spec-v2.1-proposals.md`; design project "Media graph app mockups") and two owner decisions: **no "don't remember" state** (a title you can't recall is plain `unseen` — one control, one sync rule), and the Tonight flow elicits **~10 candidate votes per participant** instead of a visible shortlist + mood-question round. Prototype-adopted additions: Home shelves with mandatory why-lines, axis-scatter exploration (UMAP demoted), solo Tonight, open-rooms discovery, Mix rating mode, decisive toggle, tap-to-tier on phones, Play-on-Jellyfin deep links, PIN-gated user switching, one-time-password user creation, the model-log rail, and a normative design language (§6.7–6.8).
**v2.1.1 (2026-09-17):** this specification is **amended in place** and is never forked into a v2.2 — amendment waves are recorded here as dated point releases, and "v2.1" is the file's name rather than a frozen edition (decision 288). `spec-v2.2-proposals.md` keeps its name and its numbering and stops calling itself provisional: proposals 1–161 are dated reasoning, citable as provenance only; entries 162 onward are numbered owner decisions, normative from the day they are taken until the amendment they mandate lands in this file. **This wave folds in** decisions 162 and 163 (whose three sentences nobody had amended), 164–166's five survivors having landed on 2026-09-03, and 167, 175, 214–227, 234, 288–303: §2's key rotation narrowed to the two rotations that exist (289); §4.1's `title.id` and §10's re-import restated as seed-once (162, 163); §4.1's mojibake rule replaced by the heuristic that ships; the MovieLens genome slice no longer imported across §4.1/§4.3/§10 (291); §4.2 and §6.2 rewritten to the round that shipped — the 54a–54h fold, which renumbers §6.2 to **eight** steps and is why the ballot is step 6, the result step 7 and solo step 8; §5.1's blend weight stated in this app's coordinates (167, 234); §5.2's fusion pointer vendored as `ARCHITECTURE-extracts.md` with its normative scope named (294); §6.3's four adopted constants written down (295); §7.3's and §11's Home Assistant seams designated rather than existing, and the playback route re-filed to M7 (290); §10's terms of use stated (292); §13's hold-out guard extended to Tonight (54b); §14 risks 6 and 7 rewritten. **The "~10 quick candidate votes" the v2.1 line above records is superseded by that fold:** the round is adaptive, median ~11 pairs, capped at 20 and escapable from the sixth. **Thirty-four amendments applied here, twenty-five verified already present** — the verified ones chiefly §12's milestone rows (181, 187–193, 199–208, 210–212, 214–226, 247–266) and decisions 164–166's five TV and wizard clauses, plus 180, 208, 219, 227 and 237. The two counts are the measurement this wave was, and a later wave restates them rather than adding to them. **Review cycle 1, same date, folded five more into this wave and the counts above are left as first taken rather than quietly grown:** decision 167's three remaining example strings in §6.0 and §6.8 (its §5.1 half had landed alone, so the file contradicted itself on the one constant a member reads); decision 169's *Ending a room*, owed to §6.2 step 2 since M4.12 while the control, its route and six tests shipped; decision 173's axis-absence clauses in §6.2 step 5 and §6.4, which say that the corpus bundle ships no axis artifact and that the split branch therefore never fires on release data; decision 304, which corrects §4.1's reason for not importing the genome without disturbing the ruling; and decision 306, which re-attributes §5.2's Davidson-with-ties form to §3, where the equations are, and restates its σ clause as the negative result that does carry. **Review cycle 2, same date, folded two more, and the counts stay as first taken here too:** decision 307, which marks §10's member-readable per-source licence route as not built rather than narrowing the clause decisions 292, 293 and 298 all make; and decision 308, which says in §4.3 that no per-arm sensitivity is fitted either. **Review cycle 3, same date, folded one more, and the counts stay as first taken here too:** decision 310, which strikes the cadence §10 had put in front of the `/data/backups` movie-data archive — `backup/movie_data` has no worker job, no route and no scheduler, and §2's `pg_dump` is the only backup this app runs on a clock — while leaving the restriction decision 292 mandated exactly as it stands. **Review cycle 4, same date, folded three more and the counts stay as first taken here as well:** decision 311, which narrows §4.1's and §4.3's genome sentences from a property of every install to a statement about what this build IMPORTS, because decision 291 stopped the import and emptied no table and `placement/features.py` still reads the rows a pre-291 install kept; §12's M4 row, whose "~10-vote round" was the last site of a fixed count the app has not had since M4.12 (decisions 175 and 214–226, in the one table an auditor reads to learn what shipped); and §6.2 step 4's z = 1.0 clause, which said no round could ever report `converged` where the sweep `tonight/round.py` publishes and `test_tonight_round.py` re-runs measures 0–2 in 20 (decision 184). **M5.1 folds three more into this wave as its fifth sitting, on the same date, and the counts above stay as first taken here too:** decision 323, which says §8 stage 1 mints a title row only on a provider id and parks an item that carries none; decision 340, which gives §8's fetcher the politeness clause the ported layer already implements — a declared User-Agent, robots.txt honoured per host and cached, per-host rate and concurrency policies carried as data — so a reviewer can fail the behaviour rather than debate it; and decision 331, which amends §12's M5 exit criterion in place to name the half `ops/m5_exit_criterion.py` will measure and gives M5.1 a row of its own, under decision 321's split of M5 into seven. Decisions 322, 332, 336 and 345 are taken in the same sitting and mandate schema, routing and admin behaviour rather than spec text; §12's M5.1 row cites them because that row is what schedules them, and §7.2 is deliberately not amended here because the ownership question it turns on is numbered 327 in `docs/milestones/ROADMAP-M5.md`, where it is still a question the owner has not answered rather than a decision anybody has taken, and M5.2 is the milestone that takes it.
**Supersedes:** `media-graph-spec_v1.1.md` — v1.1's interaction designs survive where they were validated; its modelling core is replaced by the measured architecture (below). Where the two disagree, this spec wins.
**Companions:** every non-obvious modelling decision carries a pointer into the corpus project's evidence: `ARCHITECTURE.md` (the model — its §3 and Appendix C are **vendored** into this repo as `ARCHITECTURE-extracts.md`, because a normative pointer nobody here can read is not normative; decision 294), `APP_SPEC.md` + `WATCH_NOW.md` + `LABELLING.md` + `DNA_MODEL.md` (measured product decisions), `RATINGS_PREP.md` (data provenance). `media-graph-spec_v1.1.md` is **vendored into the new repo's `docs/`** (superseded, but this spec cites its surviving interaction designs by section — v1.1 §4.3's sweep/battle prose and §5.4.5's social design are normative where cited). Schema and acceptance criteria are always inlined here; only UI prose remains by pointer.

---

## 0. What changed since v1.1, and why

v1.1 was written before the corpus existed. Since then the corpus project (this repo) built and **measured**:

| v1.1 assumption | Measured reality | Consequence for v2.x |
|---|---|---|
| "No cross-user collaborative filtering" (non-goal) | A 95M-rating collaborative backbone is the strongest single signal. Protocol-matched numbers: on the crowd learning curve the item prior alone holds ρ ≈ 0.44 flat while prior + backbone fold-in grows 0.45 → 0.50 as labels go 5 → 100; at 60 labels on the owned library the ladder reads prior 0.4221 → co-rating 0.5190 → co-rating + content 0.5267 | The crowd model is the core; the app imports its frozen artifacts |
| Aspect vocabulary to be built in 4 passes | Built: 578-term / 11-facet DNA vocabulary, extracted on 2,016 titles (824 owned) with quote-verified evidence, projected on 11,324 | The app imports it; only the incremental single-title extractor ships in-app |
| Mood: 8 axes, 8–13 questions, Bayesian posterior | Sessions are real (ICC 0.17) but the stored profile is worth **0.000** for choose-tonight; **3 shortlist-anchored answers ≈ +0.088 AUC**, and centring on the shortlist matters more than question form; within-evening re-ranking is worth 0.000 | v2.1, as rewritten by the 54a–54h fold: participants answer an **adaptive round** of item-anchored pairs — median ~11, hard cap 20, escapable from the sixth — instead of a visible shortlist + question round (owner decision; decisions 175, 214–226). The measured tilt machinery survives underneath — each vote is an item-anchored answer, and the tilt is chosen-minus-rejected DNA centred on the candidate-pool mean; no 8-axis posterior machinery |
| Group: Nash product + fairness ledger | Measured: no aggregation rule dominates plain averaging, and dominance rules cost −0.012. Zero-the-axis-and-surface is an **adopted design** (repair-explanation register), bound by the one measured rule on surfacing: a surfaced split must never ship bare (DNA_MODEL §5.3) | Average scores; zero the contested axis and *tell the group, alternative in hand*; ledger becomes an optional hidden nicety, not core |
| Tiers derived from duels only | The validated Personal Ledger fuses **four** observation arms in one likelihood — 3-class verdicts, margin-weighted duels with ties, explicit tier assignments, rewatch re-ratings — comparisons add within-liked resolution monotonically (+0.008..+0.016) at zero cost to global ranking | Tier list = learned cutpoints on the Ledger latent; drag-and-drop is an observation, not an override |
| Pair-selection heuristics for profile duels | For *profiles*, no selection rule beats random (best +0.0013, CI spans 0); for *ranking*, boundary-targeted selection does help | Random pairs for profile battles; active selection only in the tier-list queue |
| Guests: household-mean taste | Pairs-only guest models stay **below** the bare crowd chart at every tested budget (−0.060 Spearman at 30 pairs, −0.033 at 80; parity ≈ the complete 1,770-pair ordering of a 60-title pool); a 60-title pick-your-favourites grid ≈ 46 pairwise questions for ~10 taps | No stored guest model at all (decision 166): a guest is a session seat carried entirely by that evening's votes. The measurement is now moot rather than acted on — the alternative it argued for, the 60-title grid, goes out of scope with the guest profile itself. |

Two v1.1 principles are **promoted**, not replaced: *"the graph explains itself"* (now backed by the DNA naming layer with evidence quotes) and *"measured beats asserted"* (now the project's operating law, including a documented noise floor: pipeline variance 0.003–0.008 Spearman; anything smaller is a tie).

---

## 1. System overview

```
┌────────────────────────────── docker-compose ──────────────────────────────┐
│                                                                            │
│  frontend      SvelteKit PWA (static build served by backend or node)      │
│  backend       FastAPI + uvicorn  — REST + WebSocket, auth, scoring        │
│  worker        same codebase, queue consumer — sync, acquisition,          │
│                extraction, nightly refits (CPU-only torch/numpy)           │
│  db            Postgres 16                                                 │
│                                                                            │
│  volumes:  /data/pg  /data/raw  /data/artifacts  /data/cache               │
└────────────────────────────────────────────────────────────────────────────┘
        │ HTTPS via existing Traefik + Cloudflare certs (LAN + Tailscale)
        ▼
  phones (PWA, passkeys)   desktop browser   Home Assistant
```

**The model, in one paragraph** (full detail: `ARCHITECTURE.md` in the corpus project; §3 and Appendix C vendored as `ARCHITECTURE-extracts.md`). One frozen 64-d collaborative item space (**Backbone**) learned from 95M crowd ratings in the corpus project. A content encoder (**Cold Tower**, torch CPU) places titles *without* ratings into that space from their DNA + metadata — validated: it recovers 67% of the oracle's personal signal on unrated titles. Per person, a **Personal Ledger** holds one latent score per title, fed by four observation arms (verdicts, duels, tier edits, rewatch re-ratings) and refit nightly in seconds by preconditioned optimisation. Ranking = clean item prior + user fold-in over the Backbone, blended β_app≈0.2 — β is the **personal** weight here and the corpus measured the same optimum in the complementary coordinate (§5.1, decision 167). Everything the UI *says* — query predicates, conflict explanations, mood axes, explore frontiers — routes through the DNA vocabulary (the naming layer), never through raw embeddings.

**Hard constraint honoured throughout:** every in-app model update runs on CPU. Measured budgets: Ledger refit for 2 users over 839+ titles — seconds (LBFGS, 64-d); Cold Tower inference — sub-ms/title; nightly jobs — minutes. Backbone (re)training never happens in-app; it stays in the corpus project and arrives as a versioned artifact bundle (§10).

---

## 2. Deployment

- **docker-compose**, four services as above. `docker compose up` + the setup wizard is the whole install.
- **TLS/ingress:** the app itself serves plain HTTP on one internal port; the operator's existing **Traefik + Cloudflare** terminates TLS. `PUBLIC_URL` (e.g. `https://spielplan.example.tld`) is required config because WebAuthn binds credentials to the origin. The same origin must be reachable via **Tailscale** (already true in the operator's setup) so passkeys work identically at home and remote. Document: changing `PUBLIC_URL` later invalidates registered passkeys.
- **No GPU anywhere.** Torch CPU wheels only. The image must build and run on a GPU-less VM (reference: 4 vCPU, 6–8 GB RAM, 25 GB disk + caches).
- **Configuration:** env vars with sane defaults (`DATABASE_URL`, `PUBLIC_URL`, `SESSION_SECRET`, `SECRETS_KEY`, `TZ`). `SECRETS_KEY` (generated by the setup wizard / compose template) wraps a random 256-bit data-encryption key created at first boot and stored in the DB; connector secrets are AEAD-encrypted under that DEK, each ciphertext carrying a `key_id` for rotation/agility. Rotating `SESSION_SECRET` invalidates sessions only and never touches stored secrets; rotating `SECRETS_KEY` is the **operator command** `spielplan-secrets rewrap` (a declared console script over `core/secrets.py`'s `rewrap_dek`), which re-wraps the one DEK row and leaves every ciphertext and every `key_id` untouched. It is a command and not a UI action because the rewrap needs the **old** key at the same time as the new one, and a running app has no keyboard to type a value it is in the middle of replacing; there is no admin-facing rotation surface (decision 289). The app refuses to start secret-dependent connectors without `SECRETS_KEY` rather than falling back to `SESSION_SECRET`. A web-push VAPID keypair is generated at first boot and stored the same way. Everything connector-related (Jellyfin, LLM, TMDB, OMDb, Trakt) is configured **in the admin UI** and stored in `connector_config` — not env vars, because the owner explicitly wants connector setup in the admin view; env vars may *seed* connector config on first boot for automated installs.
- **Backups:** nightly `pg_dump` to `/data/backups`, rotation 14; the artifact bundle and raw store are already immutable files. Dumps contain ciphertext only — back up the env file (`SECRETS_KEY`) alongside them, or a restored dump cannot decrypt connector config.

---

## 3. Users, auth, identity

### 3.1 Accounts and roles

Local user accounts, **two roles and no others**: `admin` (full product plus the Admin view) and `member` (full product, no admin — initially Patrick and Jenny; membership is open-ended and a third member is first-class throughout). Roles are assigned by an admin in §6.6 Users, and **at least one active admin always exists** — the last one can be neither demoted nor disabled nor deleted (decision 166).

**There is no guest account.** A guest is a Tonight **session seat** on the initiator's phone: no account row, no credential, no profile, no Ledger, nothing that survives the evening. The schema states it rather than trusting it — `session_participant` admits a guest seat only as `user_id NULL` (§4.2, §6.2 step 2).

User creation (§6.6 Users): a **one-time password** is issued, the account is locked to a password change at first login, and passkey registration is prompted afterwards. An admin may reissue that one-time password as a password reset, and never sees, sets or types a member's password.

**First boot is a defined sequence, and a bundle-less app is a legal state:** the app boots with `/data/artifacts` and `artifact_bundle` empty, serving the setup wizard and admin routes; artifact-dependent surfaces render an explicit "no bundle imported" state instead of erroring. The wizard runs: create admin → optional env-seeded connector config (§2) → bundle import as the final step (the same importer the §6.6 Data tab exposes — that one page is M0 scope), **and stops there** (decision 164): member accounts are made in §6.6 Users, which is the only place they are made, and which — unlike this wizard — is still reachable on the day the household adds a third member. Member first-run onboarding then walks each phone through PWA install and push permission (§6 preamble).

### 3.2 Authentication — passkeys first

- **Primary: WebAuthn passkeys** (Face ID / Touch ID / Android biometrics) — viable because the Traefik+Cloudflare origin gives real TLS. Registration from the profile page; multiple passkeys per user (phone + desktop).
- **Fallbacks:** password login (argon2) always available; per-device long-lived session cookies after first auth (the v1.1 "appliance" feel — you authenticate rarely); optional 4-digit PIN for fast user-switching on a shared device (decision 165 retires the TV client).
- Sessions: HttpOnly cookies, 90-day sliding; admin routes re-prompt after 24 h. **A password change rotates the session it was made from:** every session for that account is destroyed, the caller's own included, and the response carries a fresh cookie for the device that made the change (decision 208).
- **Shared devices:** the account chip switches between member profiles, gated by the per-user PIN (the chip reads "member · passkey + PIN"). Logout clears the session cookie only — passkeys remain registered.

### 3.3 Jellyfin account linking

Admin view maps each app user ↔ one Jellyfin user (`GET /Users`), optional, one-to-one. The link drives: per-user played-state sync (§7.3), playback-event attribution, and the P(seen) prior in rating mode. Authentication is **never** delegated to Jellyfin (v1.1's reasoning stands: the app must work when Jellyfin is down).

---

## 4. Data model

### 4.1 Content spine (imported from the corpus bundle, then maintained by the app)

Tables mirror the corpus export (§10 manifest): `title` (canonical key: **`title.id` integer** — adopted verbatim at the one-time seed, because four read paths are id-keyed and two artifacts carry no id vector at all; every later id is Spielplan's, minted at or above 1e9 and below 2^31, and a bundle whose ids reach into that range is refused (decision 162). `imdb_id` is NULL on 21% of titles and must never be the join key), `title_meta` (multi-source, per-source rows kept — "one block = one droppable source"), `title_alias/genre/keyword/language/country/company/video`, `person`, `credit` (dedupe at read time, never at import), `award`, `platform_rating`, `review` (separate schema or DB — 312 MB with bodies). The bundle's MovieLens slice — `ml_genome_score`, `ml_genome_tag`, `ml_link` — **is not imported**: `media-graph-spec_v1.1.md:175` fixes the Tag Genome as a corpus-side validation artifact, "never shipped or imported into the app", and that restriction is **upheld by every import this build performs**: no import this build performs writes those three tables, so §4.3's 983 genome columns are zero for every title it seeds, and the Postgres tables `0003_content.sql` creates stay (decision 291). **One case the sentence deliberately does not cover** (decision 311): an install seeded before decision 291 still holds the 888,023 `ml_genome_score` rows every shipped build up to M4.15 loaded — no migration empties them, decision 162 seeds content once, and `placement/features.py`'s `_genome` still reads them — so there the block is populated. That is correct rather than a defect: those are exactly the cold-masked titles the Cold Tower is asked to place, and giving them the input the tower was fitted over is the better outcome of the two. **What that costs is real and is accepted rather than denied** (decision 304). For the titles §8 acquires it costs nothing — they carry no genome vector at all, by construction. For the corpus's own titles it is not nothing: a fifth of the 5,315 carrying a vector are not warm, which is exactly the set §5.3's sweep hands to the Cold Tower, and nearly all of those carry a cold-masked Backbone row, which §5.1 already treats as no coordinate. Their tower input loses about a third of its non-zero mass, and the Cold Tower's dropout training — which saw all-zero blocks — is why that is survivable rather than a defect. The change is recorded in `title_placement.blocks_imputed` for every title placed off an empty slice — on the pre-291 install above there is no loss to record and that column is correctly empty for exactly those titles — and read by no surface: the genome is one of the blocks §8's thinness test ignores, so no badge and no acquisition job follows from it.

**Schema rules imported from measured landmines** (validation in the importer, constraints in the DB):

1. `dna_tag` (extracted, quote-verified; 2,016 titles) and `dna_projected` (inferred; 11,324 titles) are **separate tables, never merged, never unioned**; every read joins carry a `tier` discriminator; 14,181 (title,term) pairs exist in both and must stay distinguishable. `dna_evidence` ships with the extracted tier — a tag without its quote is unfalsifiable.
2. `salience`, `confidence`, `n_sources` are **weights, never filters**. No `WHERE confidence > x` anywhere (a 0.5 cut would delete 44% of the extracted tier; union recalls 93%, intersection 67%).
3. `platform_rating` lives in a **display-only schema** the feature builder cannot import from. Aggregate platform scores are a popularity conduit and are banned as model features (measured: popularity penalty −0.010 Spearman for nothing).
4. `rating_source.id` values (1,2,3,4,7,11,21,23,26,28,31) are **frozen** — they key `fitted_cuts`, `equating_map`, and the dataset arrays. Never renumber.
5. `kind` (movie/series) is non-null, indexed, and **every ranking surface partitions by it** (measured: the unpartitioned crowd top-10 is 8/10 TV series).
6. Postgres PK fixes on import: coalesce NULLable PK components (`title_alias.region` etc.) to `''`; do **not** add UNIQUE constraints on `tmdb_id`/`trakt_id`/slugs (315/171/… duplicate values exist, mostly legitimate movie/series pairs).
7. Deny-list `%_bak%` / `%_good` tables and every stale JSONL in `data/export/` — export reads live tables only (the JSONLs predate the adjudication repairs).
8. UTF-8 everywhere; never "clean" non-ASCII (the corpus legitimately contains CJK, RTL scripts, ZWSP, emoji). Mojibake is repaired by a **conservative heuristic, not a row list**: a review body is re-encoded cp1252 and decoded UTF-8 only when it carries a marker (`Ã`, `â€`, `Â`), the round trip succeeds, the result differs, **and the repair strictly reduces the marker count** — anything that fails a step is left exactly as it arrived, because a legitimately Turkish or Vietnamese review must not be "fixed" into nonsense. Markers seen with nothing repairable are a **warning**, never a silent pass: measured over the shipped `reviews.sqlite`, 86 rows of 485,602 carry a marker and 0 repair, because the damage is a truncated sequence whose lost continuation byte is ambiguous between é/ã/á/à. The fix is upstream, and "fixed individually" was per-row knowledge this repository has never had.

### 4.2 User state

```sql
user(id, name, role, avatar, jellyfin_user_id NULL, created_at)
webauthn_credential(user_id, credential_id, public_key, sign_count, label, created_at)

user_title(user_id, title_id, state, state_changed_at, jf_synced_at)
    -- state: unseen | seen  (owner decision 2026-08-29: no 'forgotten' state —
    --   "seen, don't remember" is marked plain unseen; verdict/duel history is
    --   append-only and survives the flip)

verdict(id, user_id, title_id, value smallint, created_at, superseded_by NULL)
    -- value: 0 disliked / 1 ok / 2 liked  — the 3-class arm
duel(id, user_id, title_a, title_b, outcome, margin NULL, context, created_at)
    -- outcome: A | B | TIE ("about the same" is first-class data: 22% of random
    --   pairs are genuine ties; margin optional: decisive vs hesitant)
    -- context: profile_battle | tier_queue | tier_insert
tier_edit(id, user_id, title_id, tier smallint, via, created_at)
    -- via: drag_drop | explicit  — an OBSERVATION into the Ledger (§5.2)
    -- verdict, duel and tier_edit are append-only for every write path but one:
    --   decision 35's block-scoped Undo hard-DELETEs the verdict, duel or tier_edit
    --   row it is compensating and `rate_observation` keeps the card rather than the
    --   row, which it may because a block is committed when the FIRST observation of
    --   the next block lands — so an observation retracted inside its own block of
    --   15 never became history.  (decisions 174, 199, 35)
ledger_state(user_id, title_id, s float, sigma float, tier smallint, updated_at)
    -- nightly MAP output; displayed 0..1 via posterior CDF (the "relative
    --   0..1 weight" the owner asked for)
ledger_cutpoints(user_id, boundaries float[])   -- learned tier cutpoints; length = |tier set| − 1 (default 6, ordered ascending)
user_vector(user_id, kind, vec bytea, updated_at)  -- 64-d fold-in, mood tilt cache

session(id, room_code, host_user_id, state, kind, runtime_budget_min,
    include_rewatches, bundle_version, context jsonb, started_at, ended_at)
    -- state: open | voting | ballot | resolved | abandoned
    -- §6.2 step 1's three controls are COLUMNS and not `context`: §4.1 rule 5 makes
    --   `kind` non-null and indexed, and step 2's open-rooms row RENDERS all three,
    --   which is a query.  `context` survives for §11's launch metadata.
    -- `bundle_version` records the basis the pool was scored in (§10's invariant) and
    --   does NOT cascade: votes and outcomes are vocabulary-independent facts and
    --   survive a re-import exactly as §10 says Ledger observations do.
session_participant(session_id, user_id NULL,   -- NULL = guest slot on the host phone
    role, seat, tilt jsonb, answered_count, converged_at NULL, ended_by, joined_at)
    -- ended_by: converged | cap | escape  — §14 risk 6 wants the rate of each
    -- the key is the SEAT, not (session_id, user_id): §6.2 step 2 hands one phone
    --   round, so two guest seats in one room is the designed case and would collide
    --   on NULL; "one member holds one seat" is a partial unique index instead
    -- v1.1 §6's mu/sigma/tolerance/phase columns do NOT survive (8-axis
    --   posterior machinery deleted per §0); fairness_ledger omitted in v1
    --   (optional hidden nicety); concession/tolerance machinery deleted
session_answer(session_id, participant, seq, title_a, title_b, answer, latency_ms,
    selection, created_at, retracted_at NULL)
    -- one answer of §6.2 step 4's adaptive round
    -- answer: A | B | EITHER | NEITHER — EITHER lifts both, NEITHER lowers both and is
    --   the escape from a badly-built pool; both are first-class (decision 154)
    -- selection: adaptive | uniform_holdout — §13's guard, spelled exactly as `duel`
    --   spells it.  Hold-out pairs are drawn as a RATE from a key sealed with the pool,
    --   are used for neither selection nor stopping, and are the only data admissible
    --   for evaluating the round (decision 223).  Never accepted from a client.
    -- append-only: §6's undo retracts by TOMBSTONE, never by DELETE, because §14 risk 6
    --   says log every vote and a retraction is itself a vote
session_ballot(session_id, participant, title_id, approved bool, submitted_at)
    -- §6.2 step 6's blind approval ballot.  `session_outcome.approval_share` is derived
    --   from it and exists nowhere else, which is what makes §13's headline metric a
    --   measurement rather than a target with no instrument.
session_result(session_id, title_id, rank, slot, reserved bool, group_score,
    per_user_match jsonb, conflict jsonb)
    -- slot: finalist | wildcard | runner_up — which slot a row occupies is a fact about
    --   the slate, never an inference from its rank; `reserved` marks §6.2 step 5's
    --   opposite-pole seat so the reveal can label it (decision 220)
    -- conflict NULL is the silent night, and it is the common one
session_outcome(session_id, chosen_title_id, approval_share, participants)   -- feeds §13
push_subscription(user_id, device_label, endpoint UNIQUE, p256dh, auth, created_at, last_seen_ok)
    -- web-push targets; pruned on 404/410 from the push service
playback_event(id, source, title_id, user_id NULL, finished bool, at)
acquisition_job(title_id, stage, status, detail jsonb, updated_at)   -- §8
connector_config(name, config jsonb, secrets_encrypted bytea, updated_at)
artifact_bundle(version, imported_at, manifest jsonb)                -- §10
```

### 4.3 The artifact store (`/data/artifacts/<bundle-version>/`)

Read-only files from the corpus project, loaded at boot **when present** (an empty store is legal — §3.1), hot-swapped on bundle import (§10 swap protocol):

- `backbone.npz` — E, E_full, b_i, μ, plus the per-title support counts `item_n` (the §5.1 gate input) — from `cold_tower_artifacts.npz` + the item stats of the slimmed `content.npz`.
- `cold_tower.pt` — from `data/prep/cold_tower2.pt` (the live model; the earlier `cold_tower` run is superseded — the exporter must ship v2).
- `feature_contract.json` — the **exhaustive** definition of the tower's input: the nine content blocks in order with sizes (dna_x 433, dna_p 556, genome 983, genre 179, keyword 3,884, credit 244, country 97, award 2, meta 57 = 6,435 columns), per-column `feature_names`, then the review-text block = columns 0..63 of the 256-d SVD embedding (singular-value order) multiplied by a frozen scalar `text_scale = 1/(max|emb[:, :64]| + ε)` computed over the corpus at export time and stored in this JSON. The contract *references* the review-text SVD components (which ship as their own file) and records all placement-time preprocessing: genome zero-imputation, text truncation + scaling. The genome block is **populated by no import this build performs** — the slice is not imported (§4.1, decision 291) — so zero-imputation is the path those 983 columns take for every title this build seeds. The contract's RULE is unconditional; the PATH is not, and the two are different claims: on an install seeded before decision 291 the three MovieLens tables still hold their rows, `placement/features.py`'s `_genome` still reads them, and the block is populated there (decision 311). §8 stage 9 builds vectors from this file and nothing else.
- `content_X.npz`, `review_text_emb.npz` (+ optional SVD `components` for embedding newly arrived reviews).
- `ledger_hyperparams.json` — the tuned constants of the §5.2 recipe: anchor (ridge) strength λ (currently 3.0), BT weight λ_bt, step count, learning rate, margin-weighting flag + functional form (weights normalised as margin/mean(margin)), tie-prior initialisation δ₀ = 0.22 (thereafter fitted), b_i prior τ (or its CV grid), σ-inflation rate constant and cap. Per-user cutpoints and per-arm sensitivities are **not** shipped — the cutpoints are fitted in-app by design, and **no per-arm sensitivity is fitted either**: the two ordinal arms share one unscaled latent with free cutpoints each, so §3's `a_r` has no term in this model, and a bundle that ships a `sensitivity` key is reported and ignored (decision 308).
- `dna_vocab/v1/` (vocabulary TSVs, alias map, S matrix, adjudications) + `corrections_v1.tsv` (the credit-corrections ledger — travels with the bundle and is applied at every derive, §8 stage 3).
- `manifest.json` (fitted 3-class cut-points per source), `equating_map.json`, `seed_list.json` (the 100-title decade-stratified onboarding list), `judgement_set_v1.tsv`, `audit.json`.

---

## 5. The model layer (in-app)

### 5.1 Scoring stack (serving)

```
score_u(t) = b(t) + μ_u + w_cf · ⟨v_u, e(t)⟩            e(t) = E[t]      if rated (warm)
                                                        e(t) = gate·E[t] + (1-gate)·ê(t)  else
             b(t) = shrunk item prior; b̂(t) from the Cold Tower for cold titles
             gate = n_t / (n_t + k)                     evidence gating, k≈10
```

Blend with the crowd prior at **β_app = 0.2**. β is the **personal** weight in this codebase, and the corpus project measured the same optimum in the complementary coordinate, so the two numbers are one number under **β_app = 1 − β_corpus**: the corpus's 0.8 and this app's 0.2 (decisions 167 and 234). The "12 → 263 distinct titles" figure — exactly where per-user top-10s stop being the global chart — comes from a corpus table headed "blend beta (1.0 = crowd only)" and is quoted in that coordinate. The app does not *apply* the constant: the nightly fit searches the whole grid per (user, kind) in the orientation it serves in, so what ships is a **ceiling on the personal weight** rather than a served value — `BETA_MAX = 0.8`, a floor of one fifth under the crowd prior, CHECKed in the schema, and a fit that reaches it earned it on held-out accuracy. **Blend, never route** — a learned router was measured to capture 2–3% of the oracle gap and lose to the flat blend. No popularity penalty. Films and series ranked as **separate surfaces**.

### 5.2 The Personal Ledger (the one write-path for taste)

One latent `s_u(t)` per (user, title). Observation arms, all in a single likelihood, refit nightly (full-history MAP; seconds at this scale) and incrementally on each new observation for instant UI feedback:

| Arm | Model | Notes |
|---|---|---|
| 3-class verdicts | ordered logit, free per-user cutpoints | monotone link ⇒ a mis-placed personal threshold widens ties but cannot invert an ordering (measured inversion rate exactly 0.0000) |
| Duels | Davidson Bradley–Terry with ties; **margin-weighted** | tie prior δ from the measured 22% tie rate; margin weighting was selected by tuning, backing the "about the same" button |
| Tier edits (drag-drop, explicit picks) | K-level ordered logit, K = size of the configured tier set (default 7: F/D/C/B/A/A+/S ⇒ 6 learned cutpoints), whose cutpoints **are** the displayed tier boundaries | drag-and-drop = data, not override; the model re-fits around it |
| Rewatch re-ratings | new ordinal observation | drift signal for free |

Fusion math is normative by pointer, and the pointer is **vendored**: `docs/ARCHITECTURE-extracts.md` carries §3 and Appendix C of the corpus project's `ARCHITECTURE.md` (commit `3666eaa`), because a normative sentence nobody in this repository can read is not normative (decision 294). **What is normative here:** the four-arm likelihood of §3 (the equations live there), including its **Davidson-with-ties** form over duels — which is §3's arm 2 and not Appendix C's, whose only comparison model is the ridge anchor plus margin-weighted Bradley-Terry; and from Appendix C, the margin weighting the tuner chose, σ **never as a sample weight** (precision-weighting rows by 1/σ² measured −0.021 — the positive half of that sentence is Appendix C's rank-Gaussian target, which is not what shipped, so there is no σ in this app's likelihood to keep there and `ord_weight` is ones; σ here is the Laplace-diagonal output the badges and the queue read — decision 306), the ridge anchor on the label arm + BT perturbation, and the **ridge-Hessian preconditioner** (fixed-step GD measurably diverges on episodes containing one popular title — this is a scar, keep the preconditioner). **What this section supersedes:** §3's Crowd Head with its per-user low-rank head (R = 16 shared basis deltas) — the app ships no crowd head and no basis deltas, §4.3 carries an artifact for neither, and generalisation is via the 64-d user vector; and §3's random-walk prior on `b_i`, where the shipped model applies a **static ridge** under §4.3's τ. Every constant comes from `ledger_hyperparams.json` (§4.3), re-tunable offline in the corpus project. Per-title residuals `b_i^u` capture direct effects. Uncertainty σ per title (Laplace diagonal) drives tier badges ("A/S straddle") and the comparison queue. Freshness: after 12 months untouched, a title's σ inflates Glicko-style at rate c per √month, capped at the prior σ (c and cap from `ledger_hyperparams.json`) — the owner's "keep ratings up to date over time" requirement as ambient recalibration rather than chores.

Display: the 0..1 weight is the **empirical CDF of the user's own fitted `s` values, computed per kind** (their best-ranked title → ~1.0, worst → ~0.0) — the owner's "always-preferred → 1.0, always-rejected → 0.0" definition exactly, and stable under monotone rescaling of `s`.

**Measured expectations to encode in UX copy:** comparisons add resolution *within* the liked class (+0.008..+0.016 at 30 duels, monotone, no cost to global ranking); a decisive pick teaches more than a hesitant one; spreading verdicts across all three classes matters ~5× more than anything the corpus side can tune (a 60%-"liked" labeller gives up ~0.07 ρ) → the rating UI shows a running class balance.

### 5.3 Jobs (worker, all CPU)

| Job | Trigger | Budget |
|---|---|---|
| Ledger incremental update | every observation | <50 ms |
| Ledger full MAP refit + cutpoints + σ | nightly | seconds |
| Fold-in user vectors, blend weights per label count | nightly | seconds |
| Cold Tower placement of new/changed titles | acquisition pipeline (§8) | <1 s/title |
| Placement reconciliation: any owned title lacking a coordinate gets a feature vector built from DB data per the feature contract (absent blocks dropped — the tower's dropout training anticipates this; genome zero-imputed) and runs §8 stages 9–10 only. 19 such titles arrive with the initial bundle; thin ones (2 lack keywords, 3 lack any DNA row) are still placed, badged, and parked as acquisition jobs for M5 enrichment | bundle import + nightly sweep | seconds |
| DNA projection for a new title (per-title incremental — new code; the corpus `dna project` is deliberately wholesale) | acquisition | <1 s |
| Seen-state sync with Jellyfin | 15 min + webhook | — |
| Explore-frontier + taste-viz caches | nightly | minutes |
| Bundle import validation + hot swap | admin action | minutes |

---

## 6. Surfaces

All surfaces: responsive PWA, phone-first (48 px targets, one-handed, swipe), desktop as progressive enhancement, installable, service-worker shell cache. v1.1's throughput budgets stand: **<2 s per sweep card, <1.5 s per battle**, undo everywhere, next card preloaded.

Surface names (prototype, normative): **Home / Rate / Tonight / Rank / Map / Taste** (+ Admin).

**iOS web-push constraint (load-bearing):** on iPhone, Web Push works only for a PWA added to the home screen (iOS 16.4+), and the permission request must run inside a user gesture; iOS has no programmatic install prompt, so member first-run onboarding *guides* Share → Add to Home Screen, detects standalone mode, and **asks for push permission once**, on that phone, honouring a decline rather than re-asking (decision 180). Push is therefore always **best-effort**: every push-carried prompt also exists as an in-app banner, and sessions additionally as a room code/QR.

### 6.0 Home & Library

**M0 — the catalog:** a paginated list over `title`, partitioned by kind (§4.1 rule 5), filter/search on title/alias/genre/decade/seen-state, and the **title detail card**: metadata; credits, each person tappable → filters the library to their filmography; trailer key; platform scores (display-only schema, labelled as such); the DNA card — tags with evidence quotes, extracted/projected tier visibly distinct (§4.1 rule 1); the model line in the data voice (`b(t) 0.52 · β 0.20 · gate 0.93`); and two actions — **Play on Jellyfin** (§7.1) and **Show on map** (§6.4). §6.4's wander mode reuses this same card.

**M2 — Home (prototype-adopted):** the default surface becomes personalized shelves over the catalog. A greeting; a **pending-verdicts banner** ("You've watched *X* and *Y* recently — rate them?" → the §6.1 queue; §7.3's capture prompt given a permanent surface); then shelves, each with a **mandatory one-line why in vocabulary terms** — a shelf that cannot say why it exists doesn't ship:

| Shelf | why-line |
|---|---|
| Because you put *{anchor}* in {tier} | "shares {term} + {term} with it" |
| Top of your ledger | "clean item prior + your fold-in, blended at your fitted β (§5.1's measured optimum is 0.2)" |
| You've never watched anything *{term}* | "unvisited region of DNA space next to what you like" (§6.4 frontier as a shelf) |
| You and {other} both rate these highly | "the shared sweet spot — doubles as the Tonight prior" |
| Under 110 minutes | "for a school night" |
| New in the library | "placed by the Cold Tower — no crowd data yet" (§8 stage-10 badge) |

Search or an active person-filter switches Home into the catalog grid; clearing it returns the shelves.

### 6.1 Rating view

v1.1 §4.3's sweep/battle design survives nearly intact — it was ahead of its time and the measurements vindicated it:

- **Modes:** **Mix** (default — alternates sweep and battle), Sweep, Battle; blocks of 15.
- **Sweep:** one title card → `Liked / Fine / Disliked` + **`Not seen`** + `Skip` + persistent Undo (owner decision 2026-08-29: one seen-state control — a title you can't remember is plain `unseen`, §4.2). Verdict implies `seen`. Running **class-balance widget** with its warning copy ("Heavy on 'liked'. Spreading across all three classes matters about five times more than anything else you can do here." — the measured 5× lever). Prediction reveal strictly *after* the tap (anchoring; Cosley 2003), phrased "we'd have guessed the same" / "we'd have guessed {class}". The side rail carries the learning-curve copy ("Personal signal roughly triples from 5 to 100 labels. Aim for 50–100 in the first sitting or two.").
- **Battle:** two posters are the buttons; `Tie` (feeds the Davidson tie term); a persistent **decisive toggle** sets the margin weight (~1.6 vs 1.0) with the copy "a decisive pick teaches more than a hesitant one" (long-press stays as an optional accelerator only). Pairs drawn **at random** from the user's seen titles within verdict bands — no clever selection for profiles (measured null; the reason ships as UI copy: "Random pairs. For profiles no selection rule beats random — the clever ones pay off where the question is which of these few, not how do you rank everything: the tier queue (§6.3) and tonight's round (§6.2)." — proposal 54a, because the clause it replaced said the clever ones pay off in the tier queue *only*, which §6.2's adaptive round contradicts one section later). Corrections zone at the bottom (nothing tappable inside the poster cards), one row: `not seen: [left] [both] [right]` → sets that side `unseen`, swaps it out of the pair (`both` swaps the whole pair), writes no duel row, syncs per §7.3, covered by the persistent Undo.
- **Queue:** P(seen)-ordered (Jellyfin history, popularity, household co-seen), seeded first run from the imported 100-title decade-stratified `seed_list`. Blocks of 15; each card shows its queue reason ("queued because: 72% likely you have seen it"). Target: 50–100 verdicts in the first sitting or two — every measured curve says the model is still improving at 100 and beyond.
- Onboarding copy sets expectations from the learning curve: personal signal roughly triples from 5 → 100 labels.

### 6.2 Tonight (what to watch now)

**v2.1 redesign (owner decision 2026-08-29), as rewritten by the 54a–54h fold (decisions 175, 214–226):** the visible shortlist stage and the mood-question round are gone, and so is the fixed count. Participants answer this-or-that pairs of real candidates until the shortlist boundary is resolved — **adaptive length**: a median round of about eleven pairs at the shipped `boundary_z`, a hard cap of twenty, an escape from the sixth. The answers both decide the evening and carry the mood signal. The measured constraints that survive are marked inline; the round itself is owner-designed, not corpus-measured (§14 risk 6 — instrumented at M4.12, and `session_answer` is the instrument). **Eight steps, numbered stably**, because the coverage map cites them by number: 1 open, 2 join, 3 pool, 4 the round, 5 the shortlist, 6 the ballot, 7 the result, 8 solo.

1. **Initiator opens a session** and picks participants: members and/or N guests. Session controls: kind (film/series), a **runtime budget slider** (soft — the pool admits up to budget + 40 min; over-budget results are labelled "runs N min over"; on a **series** session the budget is **per episode** — compared with the show's per-episode runtime, never with a season or a series total — and every label that states a number on a series card says so, "fits your 60 min per episode" (decision 219)), and a **rewatch toggle** (default: exclude titles *every* participant has seen; "include rewatches" flips it).
2. **Join channels, all equivalent:** push to members' phones (best-effort, §6 preamble); **room code / QR** in the lobby; a live in-app lobby banner over the WebSocket; the **open-rooms list** — active sessions are visible to every household device ("MX-2210 · hosted by Mia · 3 min ago · Film · 60 min · skips seen") with tappable empty seats. **Guests use the initiator's phone after the initiator finishes** (hand-the-phone, sequential turns). **Ending a room.** The host, and only the host, may end a live room at any point before it resolves; the room becomes `abandoned`, leaves the open-rooms list, releases its code and refuses every further answer, undo and ballot. Nothing else ends a room that never reaches a result: opening a second room abandons only the host's rooms that nobody has started, and the settle-on-read cures only the case where every seat has already finished (decision 169).
3. **Candidate pool (internal — never shown as a step):** owned titles passing the kind/budget/rewatch filters, ranked by the **plain average** of member Ledger scores (measured: nothing dominates averaging; dominance rules cost −0.012). Guests contribute no taste term.
4. **The round (adaptive length).** Each participant answers this-or-that pairs of real candidates on their own device — "Which one tonight?" `A` / `B` / `either` / `neither`. *What is being estimated:* per participant, a **tonight score** per candidate — their Ledger score for that title, their stable taste, plus a **mood tilt** learned from this round's answers. The tilt is chosen-minus-rejected DNA **centred on the candidate-pool mean** (the measured centring lever), and it skips terms the candidate's own vector does not carry rather than centring against a zero it never had (decision 218). A participant with no Ledger — a guest, a member with too few labels — starts from the pool prior and is carried entirely by their answers; a seated *member* with no score row at all is a **named refusal** in the lobby, naming the person, rather than a seat nothing can be asked (decision 216). *How a pair is chosen:* to reduce uncertainty about **which titles belong in the shortlist**, never to rank the pool — among candidates whose posterior interval still straddles the shortlist boundary, the round picks the pair whose answer would most reduce the number still straddling it, ties broken toward the pair spanning the widest DNA axis, because a pair of near-identical titles teaches nothing about the tilt. **One pair in ten** is the uniform-random hold-out (§13) and is chosen by none of this — a **rate drawn from a stable key** rather than a fixed slot, so a round the escape ends at pair six carries hold-outs at the same rate as one that runs to the cap, and one round in eight draws none over a full twenty pairs, which is what a rate means. `session_answer.selection` records it as `adaptive` or `uniform_holdout` and is never accepted from a client. **The draw is sealed for the evening:** the pair a seat is shown at a given answer count is a function of a nonce frozen with the pool, so every read of that card returns the same pair and the same sealed token, and the hold-out stream cannot be made client-selectable by re-reading (decision 223). *Answers:* `A` and `B` separate two candidates; `either` is an equality constraint that lifts both together; `neither` is a rejection that lowers both — the strongest signal a participant can send about a live pool, the most informative answer the selection rule can receive because it eliminates two candidates at once, and the escape from a badly-built pool without abandoning the round (decision 154). Guests' pairs are not seen-filtered (their seen-state is unknown, and under decision 166 there is no stored guest profile that could stand in for it) and are selected to learn **them** — maximising information about the guest's own tilt rather than about a pool the members already discriminate — which naturally makes a guest's round a little longer. *Stopping, per participant:* the round ends when the shortlist boundary is resolved — no candidate's posterior still straddles it within `boundary_z`·σ — subject to a **hard cap of 20 pairs**; from the sixth pair a persistent **"just pick for us"** ends that person's round immediately on what is known so far. Neither the cap nor the escape is a failure state, and both are logged as `session_participant.ended_by` ∈ {`converged`, `cap`, `escape`}: §14 risk 6 wants to know how often each fires. **`boundary_z` is Tonight's own constant and is not §6.3's `straddle_z`.** The two predicates are the same arithmetic over different scales — a Ledger posterior in logit units against learned cutpoints, and a §5.1 score whose two halves are standardised to unit variance over the reference population against the rank-3/4 cut — so one σ multiple cannot calibrate both, and sharing it makes one of them a no-op. Re-measured at the shipped owned-pool scale, at z = 1.0 `converged` fires 0–2 times in 20 and the median round is the full cap, so the cap is effectively the only ending there is; `boundary_z` is 0.6, which fires 13–17 in 20 and lands the median round at ~11 pairs — the "about ten quick votes" this redesign was first described in, now an **outcome rather than a quota** (decisions 175 and 214). *Waiting:* participants converge at different points, and someone who finishes early sees the others' **progress and never their answers** — "Patrick 6/6 done · Jenny 9/~12 · Mia 4/~10 · waiting for 2". All answers stay **hidden until every participant has finished** — the blind-vote social property, preserved by construction. **A pool too small to ask anything is not refused:** admission stays at two candidates or more, and a seat with no pair to serve ends itself with its stop reason, so a pool of two or three settles to the ballot from the next read instead of waiting for ever (decision 215). *Undo:* a retraction is a **tombstone**, never a DELETE — §6's "undo everywhere" reaches the one surface where a mis-tap is otherwise permanent, and §14 risk 6 says log every vote, which a retracted vote still is.
5. **The shortlist.** Per-participant tonight scores are averaged across participants — **plain averaging**, unchanged: no aggregation rule dominates it and dominance rules cost −0.012. The round produces **three finalists and a wildcard**: the top three by group score, plus one exploratory pick honestly labelled ("a step outside your usual" — one exploratory slot in six). Guests contribute answers and no taste term. A hard split — divergent answers on the leading candidates, or Ledger divergence **D ≥ 0.20** (~14.5% of nights; below that, decide silently) — is **surfaced with the alternative in hand** ("You're split on light vs heavy — here's one of each"), never silently averaged. A surfaced split requires the §6.4 axis artifact — with no axis loaded there is no contested facet and no opposite pole to reserve, so the round returns the plain top three and no conflict copy. **The corpus bundle ships no axis artifact, so on release data this branch never fires and every D ≥ 0.20 evening is decided silently** (decision 173; the e2e fixture writes three axis files of its own, which is why the branch is testable at all). The importer's missing-axes finding is an explicit line on the §10 bundle report, not a log warning. The contested axis is **zeroed, not averaged**, and because zeroing only removes an influence it cannot by itself produce an alternative: **the third finalist slot is reserved for the highest-scoring title on the opposite pole of the contested axis**, and the reveal says so (`session_result.reserved`, decision 220). When no free finalist carries the *reference* pole, slot two is reserved as well — slot 2 to the reference pole, slot 3 to the opposite — so the slate genuinely holds one of each; still exactly three finalists, because this step fixes the count (decision 221). Copy: "You're split on {facet} — here's one of each. The axis is zeroed, not averaged." Conflict copy obeys the measured constraint (DNA_MODEL §5.3): D predicts "one of you is likely to land below your usual tonight" (AUC 0.610), never "someone will hate this" — a hard rule on the §6.6 conflict-phrasing LLM task.
6. **The ballot (blind).** Each participant taps **everything they would be happy with** among the three finalists and the wildcard — an approval ballot, not a ranking. Approvals stay hidden until every participant has submitted; then they are revealed together. The winner is the title with the most approvals, ties broken by group score. **Approval share** — the fraction of participants who approved the winner — is the number §13 evaluates the whole feature on, and `session_ballot` (§4.2) is the only place it exists. A guest seat submits here on the initiator's phone like any other seat: the ballot is keyed by seat rather than by account, and a room holding a seat that cannot submit never reaches the reveal at all.
7. **Result:** winner card — approval share, per-person match lines in DNA terms including the honest negative ("nothing here is their pull — *bleak* works against them"), and **Play on Jellyfin** as the primary CTA — plus runners-up and one **wildcard** ("a step outside your usual, honestly labelled"), with any slot step 5 reserved labelled as reserved. Votes *choose*; nothing re-ranks within the evening by predicted enjoyment (measured: worth 0.000).
8. **Solo mode** ("Tonight, for {name}"): no session row. It lands **directly on three picks and a wildcard** from the same pool ranked by the personal Ledger with no tilt — the fastest path to a film must not be slower than browsing Home — each with a one-line why ("pulls you with {terms}" / "a stretch — outside your usual") and a budget-fit line ("fits your 130 min" / "runs 21 min over"). A **sharpen this** control runs the same adaptive round against the same pool and re-ranks in place; the provenance line then reads "tilted by your N answers" instead of "unseen first". There is no ballot in solo mode: with one participant, approval share is not a measurement. A **reshuffle** control walks further down the ranking, and says so in one quiet line beside itself when the walk has come back round to where it started (decision 222).

**Guests have no profile** (decision 166). A guest is scored by their session votes alone, every night: no grid, no stored picks, no Ledger, no carry-over from one evening to the next. The measured collapse of pairs-only guest models (§0 row 8) is why a guest is never interviewed *for a profile* — and under this decision there is no profile to interview for, so the 60-title grid that measurement argued for goes out of scope along with the thing it was meant to seed. A guest's votes choose tonight's film and are discarded with the seat.

### 6.3 Rank view (the tier list)

- Per-user table/board of every rated title in tiers **F, D, C, B, A, A+, S** (configurable set; learned cutpoints, not percentile cuts — initialised from DNA_MODEL §4.5's measured quantile shape F 3 / D 7 / C 15 / B 25 / A 25 / A+ 17 / S 8 %, then learned). Badge shows tier + neighbourhood ("A — between Heat and Prisoners"); a title whose posterior reaches an adjacent tier within `straddle_z`·σ shows "A/S" and becomes queue-eligible. **One predicate, not two:** the badge and the comparison queue's eligibility are the same test at the same `straddle_z`, so a badged title is exactly a queue-eligible title and the badge is the queue's entry point — two thresholds leave a band of titles queue-eligible and unbadged, which is how the badge stops being able to open the queue (decision 295, proposal 157). **At the ends of the scale** the badge names the single adjacent tier that exists — S straddles down to A+, F up to D — and never repeats the title's own tier, which falls out of the arithmetic rather than being clamped afterwards (decision 295, proposal 76). `straddle_z` is a **badge** constant and must be tuned against the σ the fit actually produces: on a real household board the posterior sd (≈1.2) exceeds a tier's width (≈1.1), so z = 1.0 badges ~99% of the board, leaves the 70% boundary arm and the 20% exploration arm drawing from the same set, and makes the chip carry no information. The shipped value is 0.15. Any definition stated as posterior mass is the same predicate re-parameterised and inherits the same problem — at that σ the mass outside a title's own tier is at least 0.65 for every title, so a threshold below that can never exclude anything (decisions 175 and 214).
- **Filters:** genre, kind (movie/series — separate by default), decade, runtime, seen-state, DNA facet/term predicates ("show only `mood.cosy`").
- **Drag-and-drop rearrange** — the owner's requirement, implemented as Ledger observations: dropping a title into a tier emits a `tier_edit`; dropping it *between* two titles emits that edit **plus two margin-less duels** against its new neighbours. The model refits (incremental immediately, exact nightly); if the model disagrees strongly, the title's badge shows the tension rather than snapping back — the user is a data source, not a tyrant, and vice versa. **"Strongly" is the 80% credible interval:** the tension badge fires when the tier the person assigned and the posterior's 80% interval are **disjoint**. A tier is an interval between cutpoints and the posterior is an interval on `s`, so the only coherent reading of "outside" is that the two do not meet; a one-level difference whose bands still overlap is a difference and not a disagreement, and badging those would badge most of a young board (decision 295, proposal 71).
- **On phones:** tap a title (it lifts), tap a tier (it drops) — the same `tier_edit` semantics; drag-and-drop stays for pointer devices. **The lift is cancellable and legible:** the "Moving {title} — tap a tier to drop it." banner carries an explicit **Cancel**, re-tapping the lifted title also puts it down, every tier row is visibly armed while a title is lifted and non-picked titles dim. A modeless lift with an undiscoverable exit is the classic tap-to-move failure, and the three affordances together are what make a modeless interaction readable (decision 295, proposals 74 and 75).
- **Comparison queue** ("sharpen my ranking"): boundary-targeted active selection (70% posterior-straddling pairs / 20% exploration / 10% uniform-random held out for honest evaluation — the adaptive-inflation guard). ~10–20 comparisons place a new title; a motivated ~500-comparison weekend yields a defensible first cut; the first *stable* tier list arrives at ~1,500–3,000 comparisons (rating-seeded estimate — the unseeded 55%-exact budget is ~3,000 with a measured 1,800–5,800 span controlled by judgement noise σ, unmeasured for these users until §13's re-ask stream runs); ~30–50/week maintains it.

### 6.4 Map (exploration)

- **v1 map = axis scatter (prototype-adopted; replaces the UMAP layout):** titles plotted on **two user-selectable bipolar facet axes with named poles** (mood: heavy ↔ light · pacing: patient ↔ propulsive · structure: linear ↔ fractured · visual: naturalistic ↔ composed · sound: designed ↔ musical · …), with zoom/pan, three lenses (**facet colour / seen-state / match-for-you**), and a **Show on map** jump from every title card. **Axis definitions are a shipped, authored artifact**: one TSV per vocabulary-v1 facet (left pole, right pole, term → weight ∈ [−1, 1]), authored the way the register antipode ledger was, shipped flat in `dna_vocab/v1/` and **named for the facet it turns**: `<facet>.tsv`, the facet spelled as `dna_tag.facet` spells it, with the two pole names alone on the header line and one `term → weight` row per line after it (so `mood.tsv`, not `axis_mood_v1.tsv` — a stem that names a facet called `axis_mood_v1`; and `vocab_pacing_axes_v1.tsv`, which is per-term axis *coordinates*, is not one of these files). Editable in the §6.6 ledger editor. **No such file has been authored yet and the corpus bundle ships none** (decision 173), which is a statement about the artifact and not about the reader: the loader, §6.2 step 5's split and this map all read it where it is named and degrade to nothing when it is absent. Deterministic — no nightly rebuild, no Procrustes anchoring, no map shift on bundle re-import. **Binding note:** the prototype's facet set is not vocabulary v1 (it invents dialogue/tone/setting/craft and lacks place/era/sensibility/register) — everything facet-shaped binds to the real 11 facets at build time; era and sensibility need freshly authored poles. A UMAP similarity layout returns later (M7) as a fourth lens if wanted.
- **Wander:** click a title → its DNA card (tags with evidence quotes) and its neighbours by shared terms, each neighbour edge **labelled with the shared term**; follow edges term-by-term. Every connection is *nameable* — edges are DNA terms, never opaque similarity. The flywheel queue (§8.4) is docked on this surface.
- **Compositional search:** "Gladiator but with robots" = LLM parse → predicate (`has(robots) AND NOT has(gladiatorial)`) over `dna_tag ∪ title_keyword`, survivors ranked by similarity then personal score — the measured winner over embedding arithmetic (p@10 0.52 vs 0.46). The bound predicate is shown ("parse → has(robots) AND has(gladiatorial) · 3 survivors"); extracted-tier results first, projected-tier in a labelled second section (two-tier rule). Empty predicates land in the flywheel with their reason ("no owned title carries robots + gladiatorial").
- **Explore recommendations:** the *adjacent possible* — regions of DNA space near the user's liked regions but unvisited; policy from the measured explore analysis (~1 exploratory slot in 6, ranked by prior + proximity; cost ≈ −1 pp top-hit rate, honestly labelled). Also surfaced as a Home shelf (§6.0).

### 6.5 Taste comparison (any two profiles)

Compares **any two accounts** — `member` or `admin`, the only profiles that exist (§3.1, decision 166) — via a two-slot picker, defaulting to Patrick vs Jenny. Four tabs: **Facet silhouette / The seven axes / Divisive / Shared sweet spot**.

- **Facet silhouette:** butterfly/radar over the 11 DNA facets — each person's Ledger-weighted affinity, overlaid; agreement shaded, divergence highlighted.
- **The 7 shipped taste axes** from the verified axis analysis (CONTENT_TASTE §5: "Ship 7, not 8"): Demanding↔Easy, Cold↔Warm, Playful↔Weighty, Another-world↔This-world-rude, Head-trip↔Joke-delivery, Horror↔Big-franchise, Enchantment↔Speculation — each with its calibration film pair, both users plotted per axis. The 8th, Small-and-observed↔Big-and-staged, is **held back**: it carries the generosity/coverage confound (r = −0.80 with the user's mean-affinity offset) and merges into axis 1 under a row-centred refit; it ships only after re-derivation from an offset-free factorisation. (K = 8 stays the right factorisation dimension — the hold-back is a surfacing rule.)
- **Divisive-title list:** where the two Ledgers disagree most, with the disagreement *explained in DNA terms* ("Jenny likes the slow pacing; Patrick doesn't") — contestedness must beat the "crowd-divisive" baseline so it reflects *these two people*, not divisive films in general. The §6.2 copy rule applies here too: divergence copy predicts a relatively worse night, never active hate.
- **Shared sweet spot:** the region both like — doubles as the couple's watch-now prior, and as an acquisition lens.

### 6.6 Admin view (admin role only)

- **Connectors:** Jellyfin (URL, API key, library pick, user-mapping table, test button, sync now, webhook status); **LLM providers — Gemini, Anthropic, OpenAI**: per-provider key + model pick, a per-task model assignment (extraction / query parsing / conflict phrasing), and **parallel mode**: run extraction on N selected models and merge by the measured consensus rule (union with per-tag agreement as confidence — union recalls 93% vs 67% for intersection; agreement is a weight, never a filter; the numbers ship as the toggle's caption). Provider cards caption their structured-output mode (Gemini "batch", Anthropic "forced tool-use", OpenAI "strict schema"). Spend guard: per-title cost estimate before enabling, monthly cap, running meter reading "$4.12 of $25.00 this month" (corpus baseline: ~$0.005–0.01/title/pass on Gemini batch). TMDB / OMDb / Trakt keys with test buttons.
- **Data:** artifact-bundle import wizard (validate → report → hot-swap; §10), acquisition pipeline monitor (per-title stage board from `acquisition_job`), extraction queue (§8.4) with approve/spend controls, review of DNA rejects and low-evidence tags — ledger editors writing the TSV formats the corpus project already uses: `adjudications_v1.tsv` for DNA verdicts *and* `corrections_v1.tsv` for credit facts, so app-side fixes survive every future re-derive.
- **Users:** the household's whole user management, and the **only** place accounts are made (decision 166). One roster row per account carrying role · passkey count · PIN set/unset · Jellyfin link · active/disabled, opening a row editor: **create** (name + role ∈ {`member`, `admin`}), **rename**, **change role**, **password reset** (reissues the one-time password and re-arms the first-login change — an admin never sees, sets or types a member's password), **PIN reset**, **passkey list with per-credential revoke**, **Jellyfin re-link / unlink**, **disable** (`is_active = false`: every session and passkey assertion refused, the account and its Ledger kept, the user gone from switch lists, rating partners and Tonight lobbies) and **delete**. Three floors the surface *enforces* rather than advises: the last active `admin` can be neither demoted nor disabled nor deleted; an admin cannot reset their own credentials from this tab (§3.2's 24-hour re-prompt governs the rest); and a one-time password is shown exactly once, at the moment it is issued. §14 risk 4's `PUBLIC_URL` warning is repeated on this list, because revocation is felt here. There are **no guest profiles** to manage (§3.1).
- **System:** job health, queue depth, last syncs, backup status, logs.

---

### 6.7 Model log (transparency rail)

A per-user toggle (default off) reveals an ephemeral log (last ~15 events, never persisted) narrating every model write in one human-readable line: `verdict(jenny, Heat) = liked → ordered-logit arm, incremental refit 31 ms` · `tier_edit(Drive → A, via=drag_drop) + 2 margin-less duels vs new neighbours` · `session_answer(p, pair 4) = A — pool-centred tilt` · `parse → predicate has(robots) · 0 survivors → flywheel`. It is "drag-and-drop is data, not override" made visible, and the primary M2 debugging instrument.

### 6.8 Design language (from the prototype, normative)

Dark-first single-theme UI: ground `#0d0d0f`, cards `#141416`, one ember accent `#c8613a` spent on selection and primary actions. Type: Space Grotesk (display/body) + JetBrains Mono for every model number, ID and data annotation (the "data voice"). A fixed colour per vocabulary facet (11). Poster-forward 2:3 cards. The copy register is **"quiet reasons"**: every shelf, recommendation, question and conflict carries a one-line why in vocabulary terms, and model numbers appear in the data voice next to their name (`b(t) 0.52 · β 0.20 · gate 0.93`), never bare.

## 7. Jellyfin integration

### 7.1 Connector

Ported auth + field list from the corpus connector (`X-Emby-Token`, `/Users`, `/Users/{id}/Items` with the proven FIELDS set incl. ProviderIds, MediaStreams, DateCreated, UserData). Pin Jellyfin ≥ 10.9; new read code prefers the modern `/Items?userId=` route (the corpus connector's `/Users/{id}/Items` is the legacy alias). Identity: ProviderIds → `imdb/tmdb/tvdb` + `jellyfin_id`; upsert into `title` by the ported fill-never-clobber resolver. **Play on Jellyfin** (title card, Tonight winner): deep-link to the server's web player (`{jf_url}/web/#/details?id={jellyfin_id}`); direct playback is a later refinement.

### 7.2 New-title trigger (owner requirement: every Jellyfin add triggers acquisition)

- **Primary:** the Jellyfin **Webhook plugin** → `POST /events/jellyfin` (`ItemAdded`), token-authed. Debounce 10 min (library scans add seasons in bursts; series acquire per-show, not per-episode).
- **Fallback:** 15-minute delta poll on `DateCreated > last_sync` (the corpus connector is full-mirror only — the delta path is new code, flagged as such).
- Both paths enqueue an **acquisition job** (§8) per new title and mark removed titles `is_owned = false` (flag re-derived from Jellyfin, never trusted stale — the corpus flag is equivalent to `jellyfin_id IS NOT NULL` today but goes stale the moment the library changes).

### 7.3 Seen-state sync (two-way; new relative to the corpus project's read-only stance)

- **App is authoritative for explicit user actions.** `seen`/`unseen` set in the app writes Jellyfin's per-user Played flag via `POST/DELETE /UserPlayedItems/{itemId}?userId=` (the ≥10.9 route). **Jellyfin API keys are unscoped and admin-equivalent — no read-only variant exists** — so the setup copy says exactly that ("the Jellyfin API key grants full server access; this app uses it for reads and per-user Played writes only") and write restraint is enforced by this app's code alone. Preferred least-privilege write path: at user-link time obtain per-user access tokens (`POST /Users/AuthenticateByName`) for the linked users, store them in `connector_config`, and use each user's own token for their Played writes (a user token can only set its own state); the admin key stays read-only-by-policy for library/user enumeration. Costs: one-time password entry per linked user; a 401 on write → re-link prompt. The mapping is the plain boolean: `seen` → Played = true, `unseen` → Played = false (v2.1 — the `forgotten` state is gone). **Series are asymmetric.** For `kind = 'series'`, `seen` still writes Played = true on the Series item, but `unseen` is app-only and is never written: Jellyfin executes `DELETE /UserPlayedItems/{seriesId}` as a recursive `MarkUnplayed` that zeroes `PlaybackPositionTicks`, `PlayCount` and `LastPlayedDate` on every episode — history this app never owned and cannot restore. The row is stamped `jf_synced_at` so the sweep does not re-owe the write, and the surface says the change is app-only. (Decision 210.)
- **Jellyfin playback is a suggestion, never a silent write:** ≥90% playback (poll `/Sessions` + `IsPlayed` delta) arms a per-user prompt — "Did you finish X?" → one tap sets `seen` and offers the verdict flow (v1.1 §4.1 capture, kept). Push notification if the user isn't in the app — best-effort (§6 preamble); when undeliverable, the prompt queues and surfaces as an in-app banner on next open. The banner path is the whole M1 behaviour; push arrives with the M4 stack. **A sweep never answers the question it asked.** While a finish prompt for a (user, title) is open, the 15-minute seen sync leaves that row alone; only a tap closes it — `yes` writes `seen`, `no` writes `unseen`, both as explicit user actions under the conflict rule below. (Decision 211.)
- Conflict rule: last-writer-wins with the app's explicit action outranking Jellyfin's inferred state; `jf_synced_at` prevents loops. A **series** Played flag is computed by Jellyfin from its episodes (`played >= total`, and `true` for an empty folder) rather than asserted by a person, so it never overrides an app-side row in either direction: Jellyfin played over an *absent* row is adopted, because an absent row is a default and Jellyfin's history is real history; Jellyfin not-played over an app `seen` is ignored, because a show does not become less watched when a new season lands; and Jellyfin played over an app `unseen` is ignored too, because `unseen` on a series is app-only by decision 210(a) — the app never sent it, so the folder's disagreement is the design and not news. (Decisions 210 and 213.)
- **External playback events** (v1.1 §6.1, kept verbatim): `POST /events/playback` token-authed, for the theater path that bypasses Jellyfin — the designated Home Assistant seam (§11). **It is not built, and it is not part of §12's M1 row:** `playback_event.source = 'external'` exists in the schema (`0005_ledger.sql`) and no router serves the route, so §13's rating-capture input for a film watched outside Jellyfin has no producer. **M7 owes it**, with §11's other two seams (decision 290).

---

## 8. New-title acquisition workflow

Ported skeleton: the corpus project's raw store (content-addressed, immutable — crawl once, re-parse forever), durable `(kind,key)` queue, per-host rate-limited HTTP layer, and the single-title prototype (`mdc probe`). New code: per-title incremental derive (the corpus `rebuild`/`project`/`ingest` are deliberately wholesale).

**Pipeline per title** (stages visible in the admin board):

```
1  identify      Jellyfin ProviderIds → title row (fill-never-clobber); a row is
                 MINTED only on a provider id (imdb/tmdb/tvdb) — an item Jellyfin
                 supplies no provider id for parks here with the reason "no provider
                 id" and mints nothing, because a name-and-year mint is the wrong
                 match the resolver already refuses (decision 323)
2  enrich        tmdb:resolve → tmdb:detail (metadata, credits, keywords)
                 wikidata:resolve (→ MC/RT/Letterboxd slugs — halves guessing)
                 omdb:detail · trakt:summary→comments · wikipedia:article
                 tvmaze:show (series) · rt:page · metacritic:page→reviews
3  derive        per-title parse of raw docs → title_meta/credit/review/…
                 (single-title version of the corpus rebuild, keyed entity_key);
                 ends by applying BOTH curated ledgers — adjudications_v1.tsv
                 and corrections_v1.tsv — exactly as the corpus rebuild runs
                 corrections last: a derive that regenerates rows without
                 re-applying them silently reverts curated fixes (§14.5)
4  reviews gate  pack requires plot + multi-source reviews ≥50 words; if thin,
                 retry window 30 days (new releases accrue reviews over weeks)
5  dna pack      ported packs.py (interleaving, caps, norm()) + craft supplement
                 (wikipedia craft sections; NOTE: the RT critic-blurb pool is a
                 frozen 2020 dataset → genuinely new releases get thinner
                 sound/visual facets — known gap, mitigation: wiki sections +
                 metacritic critic excerpts, revisit if coverage measures poor)
6  dna extract   LLM structured call(s) against the frozen vocabulary —
                 1..N providers per admin config; two passes recommended
                 (union +13pp recall); ~20–27k input tokens/pass/title
7  verify        ported trust boundary verbatim: term-in-vocabulary (after
                 alias repair + adjudication rename), quote-substring-of-pack
                 via norm(), salience ∈ {1,2,3}. 100% catch rate on fabricated
                 tags in the corpus pilot. Failures drop, never repaired.
8  project       per-title alias-map projection of its keywords (incremental —
                 new code, same alias map)
9  place         feature vector per the feature contract → Cold Tower →
                 ê(t), b̂(t); genome block zero-imputed (unavailable for new
                 titles by construction)
10 ready         appears in ranking/search/explore with a "new — model
                 placement, no crowd data" badge until ratings accrue
```

Failure at any stage parks the job with a reason, retryable from admin; paid stages (6) never auto-retry past the spend cap. All fetched bytes land in the app's own raw store, so re-parsing is free forever.

**The fetcher is polite, and says so in a form that can be failed (decision 340).** It declares a User-Agent naming the app; it honours each host's `robots.txt`, fetched per host and cached in Postgres; and it carries per-host rate and concurrency policies as data rather than as constants, so §6.6 can show them. An override of a host's robots.txt is permitted only where it is documented with its reasoning beside the policy it changes. The household's own Jellyfin server is exempt: it is reached with the household's own key and is not a third party to be polite to.

### 8.4 The extraction flywheel

A queue of *naming failures* that future LLM spend should fix, fed automatically: compositional queries hitting empty predicates, explore frontiers with no vocabulary coverage, titles whose "unnamed taste" residual share is high, thin-facet titles. Admin reviews the queue, picks a batch and providers, sees the cost estimate, launches. This is how DNA breadth grows *where it's needed* — breadth being the measured binding constraint (zero owned titles carry `themes.robots`).

---

## 9. LLM connector layer

Ported design (no vendor SDKs; one POST per provider through the rate-limited fetcher; batch endpoints supported):

- **Providers: Gemini, Anthropic, OpenAI** — the three adapters exist in the corpus project with the per-provider structured-output quirks already solved (Anthropic forced tool-use; OpenAI strict-schema keyword stripping; Gemini responseSchema + header-only API key so credentials never hit logs). Port verbatim, including the reasoning-token accounting corrections (Gemini bills thinking tokens as output — counting visible JSON understates cost ~5×).
- **Admin-configurable:** per-provider key/model, per-task assignment, **parallel/consensus mode** (N models extract independently → union-merge with agreement-as-confidence, the corpus pass-0/pass-consensus pattern), batch-vs-sync toggle, spend caps + meter.
- **The schema is a cost-saving device, not the guarantee — the guarantee is the validator** (ported principle). Two-attempt pattern: retry once with the specific contract violation named.

---

## 10. Data migration (corpus project → app)

**The bundle** (produced by an export script in the corpus project; versioned; the app's importer validates then loads):

| Part | Contents | Size |
|---|---|---|
| `content.sqlite` (or pg_dump) | title spine, meta, aliases, genres, keywords, credits/people, awards, platform ratings (display schema), DNA layer (tag + projected + evidence + annotation + term_signal + exclusion), genome + link slice (shipped in the bundle and **not imported** — §4.1, decision 291), `rating_source` (mandatory always, and the carrier of the per-source licence terms — §6.6's admin **Data** card is what reads them out; decision 307), `rating_title_map`, seed_list tables, watchlist | ~750 MB |
| `reviews.sqlite` | all 485,602 reviews with bodies (needed for future re-extraction and text embedding) | ~312 MB |
| `artifacts/` | backbone + item stats (slimmed `content.npz`) + cold tower (`cold_tower2.pt`) + feature contract + content_X + review-text emb (+ SVD components) + `ledger_hyperparams.json` + calibration files (`manifest.json` cut-points, `equating_map.json`, shares grids, `audit.json`) + `dna_vocab/v1/` + `corrections_v1.tsv` + `seed_list.json` + `judgement_set_v1.tsv` | ~60 MB (+200 MB with text components) |
| *(optional, later)* `ratings.npz` | the cleaned 94.8M-row corpus — only if in-app retraining is ever wanted | +2.4 GB |

Total ≈ **1.15 GB** uncompressed (~0.35 GB compressed). Importer enforces every §4.1 landmine rule and produces a migration report (counts per table, validation failures, vocabulary version). **Model re-import (retrained backbone) is a planned admin event with a migration report** — never a silent sync, and never a diff report, which does not exist. **Content seeds once** (decision 162): the re-import that arrives carries models, not content. **A vocabulary change is refused until a migration plan exists** (decision 163, §14 risk 7). Ledger *observations* always survive re-import (they reference `title.id` and vocabulary-independent facts) — but the rebuild set is larger than the DNA caches: **everything expressed in the old Backbone's basis is garbage against a new one**. Re-import therefore recomputes: user fold-in vectors (closed-form, ms), per-label-count blend weights, a full Ledger MAP refit, Cold Tower re-placement of every app-acquired title (feature vectors rebuilt from the staged bundle's feature contract, whose column set may change). (The v1 Map is a deterministic axis scatter and needs no rebuild — a future UMAP lens would recompute here.) Swap sequence: validate → stage to `/data/artifacts/<version>/` → recompute the rebuild set against the staged bundle → transactionally flip `artifact_bundle.active` → restart backend + worker (simplest correct option at household scale). Invariant: no process may score or refit with a loaded bundle version different from the active row.

**The bundle is private household data.** It is assembled under personal and non-commercial terms — verbatim MovieLens tables, scraped IMDb reviews and IMDb-derived tables, Metacritic and Trakt bodies whose own `rating_source` rows read "not redistributed", whole critic articles, MPST synopses, OMDb CC BY-NC text and TMDB content — so it is **never published with a release, never shipped as a release asset and never handed to another household**. The `/data/backups` movie-data archive inherits the same restriction, and `LICENSE` (MIT) covers this repository's code and nothing in the bundle. The per-source terms the app has to display travel with the data, in `rating_source`'s `url` / `license` / `version` / `notes`, and the surface that reads those four columns out today is §6.6's admin **Data** card. /account's **Data sources** block carries this app's own notices — IMDb's courtesy line, TMDB's "not endorsed, certified, or otherwise approved by TMDB", CC BY-SA credit for Wikipedia and TVmaze, OMDb's CC BY-NC — in §6.8's quiet data voice, one notice on one surface and never a source name on every card (decisions 292 and 293). **A member-readable route for the per-source terms is not built** (decision 307): the one route serving those columns takes `AdminUser`, so a household's non-admin member cannot reach the eleven datasets' own licence text, and `docs/RELEASE.md` section 5 carries that as an unmet promise rather than this file implying it is kept.

**Corpus-project deliverable to build:** `mdc export-bundle` implementing this manifest — deny-listing `%_bak%`/`%_good`, live tables only, `content.npz` slimmed by dropping the per-user affinity arrays (`aff` + `aff_den`, ~1.29 GB) while keeping the item stats (incl. `item_n`), filtered `imdb_ratings`/`ml_link` joins, both curated ledgers, the feature contract with its frozen `text_scale`, and the tuned ledger hyperparameters persisted to `ledger_hyperparams.json` (today the tuner only prints them).

---

## 11. Home Assistant (later; independence guaranteed)

The app is fully standalone; HA integration is additive via three **designated** seams, **none of which is built**, each owed by **M7**: `POST /events/playback` (theater finished a film → arm rating prompts); presence hints for session lobbies (optional `person.*` states → suggested participants); and a webhook/service to launch a watch-now session from an HA dashboard (returns the session URL/room code; phones join via push as normal). What exists today is where they will land and nothing more: `playback_event.source = 'external'` (`0005_ledger.sql`) and `session.context jsonb` (§4.2) are the two landing places, the app registers no `/events` router, and there is no presence input to the lobby. "Existing" was the wrong word and is the defect this amendment closes: nothing in the app waits on these seams, and nothing in the app provides them (decision 290). No HA dependency anywhere in core flows — which is the promise, and it is kept.

---

## 12. Build order

| M | Contents | Exit criterion |
|---|---|---|
| **M0** | Compose skeleton, Postgres schema, first-boot wizard (§3.1 sequence), auth (passwords + sessions), bundle importer + validation report, artifact loading, Library view (§6.0) | bundle imports clean; Library list and title card render imported titles |
| **M1** | Jellyfin connector + user linking + seen-state two-way sync (finish-prompts as in-app banners; push waits for M4); passkeys. §7.3's `POST /events/playback` is **not** in this row — §11 designates it and M7 owes it (decision 290) | seen states flow both ways for both users |
| **M2** | Rating view (mix/sweep/battle, class balance, undo, queue, seed list) + Personal Ledger (all four arms) + nightly refit; scoring stack + per-user ranked lists; **Home shelves (§6.0)**; model-log rail (§6.7); member PWA-install/push onboarding; placement reconciliation (§5.3) | 50–100 verdicts each produce visibly personal rankings — the first real-user validation of the whole corpus project; every owned title has a coordinate (warm Backbone row or Cold Tower placement) |
| **M3** | Rank view: tiers, filters, drag-drop, comparison queue | stable tier lists both users endorse |
| **M4** | Tonight: lobby + open-rooms discovery, push join, the adaptive round (median ~11 pairs, cap 20, escape from the sixth — the "~10 quick votes" this row was first written with is superseded by the 54a–54h fold, decisions 175 and 214–226), guest hand-off, group combine + conflict surfacing, blind reveal, **solo mode** | a real Friday night resolved by the app |
| **M4.6** | **User management (§6.6 Users):** the roster and its row editor — create / rename / change role / password reset / PIN reset / passkey revoke / Jellyfin re-link / disable / delete — the two-role account table, and account creation moved off the first-boot wizard (decisions 164, 166) | an admin adds a third member, resets that member's password, then disables and deletes them, from the Users tab alone; no account can be created anywhere else, and the last admin cannot lock the household out |
| **M4.7** | **The box (§2 Configuration + Backups, §5.3 jobs, §1 image):** required config that refuses to boot rather than falling back; secrets custody that degrades instead of 500ing, with a rotation and repair CLI; an honest `/api/health`; persisted job outcomes and a nightly that means a night; a restore and an upgrade the operator can follow; and an unprivileged CPU-only image whose declared console scripts exist (decision 181) | a dump restored by the README procedure boots and serves, and the same dump under a changed `SECRETS_KEY` boots too - every member write still commits with a reason naming the key, no route 500s, and the operator can re-enter the credential from the UI |
| **M4.9** | **From the corpus to the card (§4.1 spine, §4.3 vocabulary, §6.0 catalog + title card, §6.6 Data, §6.7 rail):** one facet vocabulary end to end — loader, migration backfill, SQL predicate, chip, palette; a title card that names what it truncates; a catalogue whose pagination and search are total and literal; two authenticated routes that partition by kind; a Home whose anchor and badges tell the truth; §6.7's rail reachable from every surface; and four loader tables that stop being dropped or misreported (decisions 187–193) | against the real export bundle: every `dna_tag` and `dna_projected` row carries a facet the eleven-facet vocabulary knows, every title card renders past its credits and prints how many it hides, the catalogue paged end to end under a concurrent rewrite returns each title exactly once, no projected DNA term outranks an extracted one, and no shipped table is skipped unaccounted |
| **M4.10** | **The verdict path and the tier board (§6.1 Rate, §6.3 Rank, §4.2 append-only, §7.3 push, §5.3 budgets):** every Personal-Ledger write on both surfaces made exactly once under a lock and refused with §6.1's 409 rather than a 500; neither surface raising after its observation is durable; the Jellyfin round trip outside the verdict transaction; a tier-set change that survives a concurrent refit; §6.1's prediction reveal available in the first sitting; §13's hold-out arm gated and the sealed pair stable; and the exploration arm, the straddle badge and the queue's copy saying what the model actually did (decisions 199-208) | two simultaneous taps on one card token, and two simultaneous answers under one sealed pair, each leave exactly one observation and one refusal that names why, eight repetitions running; with the refit forced to refuse and a 300-character title name no Rank or Rate route raises after its row has committed; and a member's first sitting gets a prediction reveal on every tap but the first |
| **M4.11** | **Seen states both ways, for a real household (§7.3 two-way sync, §7.1 identity, §7.2 ownership, §4.2 `push_subscription`, §5.3 budgets):** a sweep that adopts for a tokenless link instead of aborting at its first owed row, counts every failed Played write instead of reading as healthy, and clears a re-link badge only on a write that actually succeeded; one seen decision per title across duplicated copies; a series never marked unplayed recursively and never un-marked by a recomputed folder flag; ownership falsified when the library drops a title; a finish prompt that arms once for television, stays answered, and offers §6.1's queue; and push that reaches a member's second device under a tag naming its kind (decisions 210-212) | two members, a library with a duplicated copy and a running series, two phones each: an explicit "not seen" survives two sweeps with nothing adopted and both copies clear, a new episode does not un-mark the show, a revoked token stays flagged while a 404 on every write is counted rather than hidden, the prompt arms once on an Episode session and stays answered, and both notifications carry a distinct tag and a destination |
| **M4.12** | **Tonight, end to end on the real pool (§6.2 in full, §6 preamble's 1.5 s per battle, §3.1/§3.2 on the session channel, §13's hold-out, §4.2's session tables):** one lifecycle owner, so a room that stops progressing settles from the next read instead of waiting for ever; a claim taken on the room before the pool is built, and a seat that can never be asked anything ending itself rather than stranding; every seat write serialised, so two taps on one card token are one 200 and one 409 and never a 500; a pair search affordable at 696 candidates and a stopping boundary that is Tonight's own constant rather than §6.3's badge one; a hold-out sealed with the pool and drawn as a rate; a split slate that is still true when the leading finalist carries no pole, with its reserved card labelled; a session socket behind the dependency graph that holds no pooled connection while it writes; a guest seat that reaches the blind reveal on the initiator's phone, on a device that remembers which seat it is playing; an answer latency that is a measurement; and no TV client (decisions 214-226) | a real evening resolves on the shipped 696-title owned pool: two members and one guest seat on one phone reach the blind reveal with an approval share over three seats and one `session_outcome` row; a guest seat's first pair, and a zero-label member's first pair and first answer, are each under 1.5 s; an unrelated read from a second cookie answers while an answer is in flight; a `play.finish` made to raise once still reaches the ballot on the next read of the session; and pools of exactly two and exactly three candidates reach the ballot rather than waiting. Measured end to end by `ops/m412_exit_criterion.py`, which refuses to run on a fixture pool |
| **M4.14** | **Bundle import and artifact custody (§5.3's import job, §10 swap sequence + migration report, §6.6 Data, §4.3 `artifacts/`, §4.2 `artifact_bundle`):** the import made the worker job §5.3 files it as, so the request returns in seconds with a version to poll while the load, the rebuild set and the flip run off the event loop; an archive door that opens a `.tar`/`.tar.zst`, refuses a truncated or traversing one, and leaves no reusable tree behind; a `DATA_DIR` boundary that is a path rather than a string prefix; every file `BUNDLE.json` lists sha256-verified before the first row is written; referential integrity, the deny-list, the shipped-view count and the artifact readers reporting rather than raising; one derivation of the vocabulary version and decision 163's refusal made reachable when the bundle declares none; the curated ledgers a models-only bundle carries actually loaded; a backbone allowed to cover corpus titles this install never seeded but never allowed to cover fewer; two concurrent imports serialised; a broken install that says so and can be restaged; and one rule for deleting an `artifact_bundle` row, held by the schema (decisions 247-266 and 287) | a 1.04 GB real-bundle archive imports through the Data tab with `POST /api/admin/bundle/import` returning in under 5 s and progress polled to completion, surviving a proxy that cuts at 100 s, with `/api/health` answering throughout; every file listed in `BUNDLE.json` sha256-verified before a single row is written; and each of a truncated archive, a plain file under `/data/import`, a bundle staged under `/data/artifacts/<version>/`, a missing vocabulary, an orphan row, an unsafe version token and a second concurrent import ending as a report line with the install byte-identical to before — no reusable `.unpacked-*` tree, no half-staged version directory, no 500. Measured end to end by `ops/m414_exit_criterion.py`, thirteen numbered checks, which refuses to run on the fixture |
| **M5.1** | **The acquisition spine (§8's ported skeleton, §5.3's job budget, §6.6's board):** the durable `(kind,key)` queue, recovered by lease expiry rather than by any shutdown handler; the content-addressed raw store that makes a bad parse cheap to fix instead of expensive to repeat; the polite per-host fetcher; and the ten-stage park-and-resume driver — wired to the two §8 stages that already ship with no caller, placement's `app_acquired` scope and Home's "New in the library" shelf, with stages 2-8 declared no-ops the six milestones below fill (decisions 321, 322, 323, 331, 332, 336, 340 and 345) | with stages 2-8 declared no-ops, a task injected for a Jellyfin item carrying provider ids walks stage 1 to 9 to 10: it mints a title above 1e9 with `origin = 'acquired'`, is placed by the Cold Tower and appears on Home carrying the "new — model placement, no crowd data" badge. A worker killed mid-lease has its task reclaimed by the next worker and completed exactly once, with no duplicated derived row; one URL fetched twice writes one file under `/data/raw` and two `raw_document` rows, and the second parse issues no request; an item with no provider id parks at stage 1 with that reason and mints nothing; and `/data/raw` is readable by the worker and absent from the backend container. Measured end to end by `ops/m51_exit_criterion.py`, twelve numbered checks, which refuses to run on the fixture |
| **M5.3** | **The sources and the derive (§8 stages 2-4, §14 risk 5, §14.5):** the eight source fetchers §8 stage 2 names, every one of them through M5.1's polite per-host fetcher and every response in the raw store before anything parses it; the parsers, which import no transport at all, so a bad parse is re-run from those bytes instead of re-crawled; the per-title incremental derive that replaces the corpus project's deliberately wholesale rebuild; the two curated-ledger appliers — `adjudications_v1.tsv` at ingest and `corrections_v1.tsv` last, at their own points rather than in one merged pass, which is what §14.5's scar is about; and stage 4's reviews gate, whose shortfall is a 30-day park rather than a failure (decisions 326, 334, 335, 346 and 372-378) | a title carrying one of the bundle's `credit_correction` rows is re-derived from its raw documents and still carries the correction afterwards, with the DNA verdicts applied at ingest and the credit corrections applied last; deriving it a second time changes nothing. A title with a plot but fewer than two review sources parks at `reviews gate` with both counts in its reason and a 30-day window, and the admin retry resumes from that stage rather than from stage 1, so nothing before the park runs again: no request leaves the process, no HTTP client is constructed, and no derived row is duplicated. A source that 404s is a note on the job while a failure of the required source parks with that source named; a scraped page that belongs to a different film is refused with nothing written; and a models-only re-import afterwards leaves a household-authored correction in place. Measured end to end by `ops/m53_exit_criterion.py`, eleven numbered checks, which refuses to run on the fixture |
| **M5** | Acquisition pipeline + admin connector UI + LLM layer + extraction flywheel | a new Jellyfin add reaches "ready" unattended — and, measured with it by `ops/m5_exit_criterion.py`: the flywheel appends a naming failure at the moment it happens and an admin launches exactly the batch they selected; the spend cap parks a paid stage with its reason instead of billing for it, and never auto-retries past it; a provider response that violates the extraction contract is retried exactly once with the violation named, and a second violation fails the stage and writes nothing; a re-derive of a title carrying a curated correction still carries it afterwards; and a burst of adds for one series yields one job for the show rather than one per episode (decision 331) |
| **M6** | Map (axis scatter) + compositional search + taste comparison viz | — |
| **M7** | Acquisition/"wanted" lens, UMAP similarity lens, HA hooks, comfort-shelf carryover | — |

M2 is the gate — and the moment the corpus project's biggest open caveat ("does any of this transfer to two real people?") gets its answer. Instrument it: per-user held-out accuracy from day one, compared against the crowd-measured expectations.

**M4.6 is not in the original build order.** It exists because §6.6 sketched user management in a single line and this table scheduled it nowhere, while the first-boot wizard — the only path that creates an account today — becomes unreachable the moment an admin exists (`required = not has_admin`). A two-member household that wants a third member has no in-app way to get one, and an admin who forgets a password has no way to reissue it. It is numbered M4.6 rather than folded into M4 or M5 because M0–M4 are shipped rows and M4.5 is closing; `docs/TESTING.md` already establishes a milestone outside this table as a legitimate shape.

**Neither is M4.7, and for the same reason one step down.** §2 promises required configuration, secrets custody with rotation, a nightly dump with rotation 14 and a restore; §5.3 lists the jobs; §1 pins a CPU-only image on a box that also runs Postgres. This table scheduled none of it, so every one of those promises is documented, coverage-mapped, and either unimplemented or implemented in a way that fails on the day it is needed — a restore that loses the connector row and the push keypair, a `SECRETS_KEY` mistake that 500s the route which would fix it, a health check that reports a dead database as healthy, and a backend that emits no INFO log at all. M4.7 is that work, and decision 181 records the call. It runs before M5 rather than inside it because it also owns two seams later milestones plug into: the app-level exception-handler block, and the worker's `job_run` table.

**Nor is M4.9, and unlike those two it is a row this table already had — asserted against the wrong artifact.** M0's exit criterion is "bundle imports clean; Library list and title card render imported titles", and it was closed against a fixture in which the corpus's awkward shapes do not occur. M4.5 then made the real bundle *importable* and never asked what the rows look like once they are in. They ship two department spellings for one job, two facet namings for one vocabulary, and a term id that already carries its own facet — and the whole chain that reads them, `importer/*` → `db/library.py` → `home/*` → the cards, was written against a fixture where none of that is true. The result is one data shape guessed differently at each layer: the title card throws inside the client render on 1,216 of 19,071 real titles, 29,188 of 31,540 extracted DNA rows carry a facet that joins nothing, the catalogue pages over a non-total order, and four shipped tables are dropped or misreported. M4.9 is that repair, and decisions 187–193 record the calls it needed. It runs before M5 because M5's flywheel writes into exactly these tables, and because a household cannot use the app until the card it taps opens. (M4.8, which precedes it, takes **no row here**: it repairs the instrument rather than a promise this table made — see decision 183's preamble.)

**Nor is M4.10, and it is M4.9's shape: two rows this table already had, closed against writes that had never been made twice at once.** §12's M2 row owns the rating view, the Personal Ledger and §6.1's anchoring-safe prediction reveal; its M3 row owns Rank's tiers and its comparison queue. Both were asserted one request at a time, and both surfaces guard every Ledger write with a read, then some work, then a write, on a connection that autocommits each statement. Measured: two gathered answers under one sealed pair write two `duel` rows in most races with no injected latency, one of those races drawing §13's held-out arm — the only data §13 admits to evaluate the tier model, in a table §4.2 makes append-only on a surface with no undo; two gathered taps on one Rate card token leave the loser holding a refusal §6.1 does not define — since M4.7's conflict handler a 409 naming the `rate_observation_seq` index, which the client has no rule for — with its Jellyfin write already sent; a 1.5 s push holds one backend `idle in transaction` for the whole wait against a pool of ten, because the round trip is awaited inside the verdict transaction; both Rank routes raise after their observation is durable, so every retry writes another append-only row; and §6.1's reveal was dark on 50 of 50 taps for both members on the real bundle — exactly the sitting M2's exit criterion is defined over. M4.10 is that repair, and decisions 199-208 record the calls it needed. It runs before M5 because M5's flywheel and §13's evaluation both read these tables, and because the two surfaces a household touches every week are the two that lose taps.

**Nor is M4.11, and it is M4.9's and M4.10's shape once more — the row above it, asserted against a household this table does not describe.** §12's M1 row is "Jellyfin connector + user linking + seen-state two-way sync (finish-prompts as in-app banners; push waits for M4); passkeys", and its exit criterion is "seen states flow both ways for both users". It was closed against one member, one copy per title, no series and no second phone. Against two members, a library that ships 'Movies' and 'Movies 4K' copies of the same film, and a show whose next season lands: a link made without Jellyfin credentials aborts that member's sweep at the first owed row and imports none of their history, permanently; a duplicate copy marked Played reverts the explicit "not seen" the person typed; `DELETE /UserPlayedItems/{seriesId}` is a recursive `MarkUnplayed` that erases every episode's resume position, and the same computed folder flag un-marks a show the day it grows; a title deleted from the library keeps `is_owned = true` and stays in Tonight's pool behind a dead deep link; a revoked token is promoted back to "linked" by any sweep with nothing to push, while a 404 on every write reads exactly like a healthy sweep; and no Episode session arms a prompt at all, because the test double emitted a shape no server sends. M4.11 is that repair, and decisions 210-212 record the calls it needed. It runs before M5 because M5's acquisition pipeline consumes this sweep's unmatched report and its ownership flag, and because §7.3 is the half of the app that has to be right while nobody is looking at it.

**Nor is M4.12, and it is M4.9's, M4.10's and M4.11's shape a fourth time — the row this table has had the longest, closed against a fixture that could not express an evening.** §12's M4 row is "Tonight: lobby + open-rooms discovery, push join, the adaptive round (median ~11 pairs, cap 20, escape from the sixth — the "~10 quick votes" this row was first written with is superseded by the 54a–54h fold, decisions 175 and 214–226), guest hand-off, group combine + conflict surfacing, blind reveal, **solo mode**", and its exit criterion is "a real Friday night resolved by the app". It was closed against a six-title candidate pool, a household of one account, and a guest seat that could answer a round but had no control that could submit its ballot. Against two real accounts and the shipped 696-title owned pool: the pair search costs 36 s for a member and 126 s for a guest seat, against the §6 preamble's 1.5 s per battle; the `voting → ballot` transition lives only inside the answer handler, so one transient failure leaves the evening in `voting` for ever with its data intact and unreachable; a pool of two or three candidates converges at zero answers and no route can close the room; a member who joins while the host is building the pool is seated into a snapshot that has no scores for them and deadlocks the same way; nothing anywhere can end a started room, so every other stall is permanent and recovery means SQL; two answers gathered on one card token are a 500 rather than a 409, and an undo interleaved with an answer makes every later tap a 500 for ever; the session WebSocket authenticates outside the dependency graph, skipping §3.1's first-login lock, and awaits its first two sends inside an acquired connection, so ten stalled sockets pin the whole ten-connection pool and the backend stops answering; a room with any guest seat can never reach the reveal at all; and every `session_answer.latency_ms` ever written is 0, which is the instrument §14 risk 6 requires before anyone re-tunes the round. M4.12 is that repair, and decisions **214-226** record the calls it needed — among them decision 175's boundary retune, deferred here by decision 205, and decision 165's TV retirement, which is why this table's M4 row loses "(+ TV route)" in the same change. It runs before M5 because §12's M4 criterion is the one row here a household checks by living through it, and an evening that cannot end is not a row anything later can be built on.

**Nor is M4.14, and it is M4.6's and M4.7's argument and M4.9's at once (decision 259).** §5.3 files "Bundle import validation + hot swap" as an admin-triggered job with a minutes budget, and it is not one: it is a 127-second `await` inside `POST /api/admin/bundle/import`, on the backend's event loop, inside a single transaction, with the CPU-bound rebuild on the request loop — behind an ingress that cuts a proxied request at 100 s, so the operator is told the import failed while it completes and flips, and the retry hits decision 162's seed-once refusal, which reads as corruption. This table scheduled none of that, which is M4.6's and M4.7's shape. The other half is M4.9's: this table's M0 row owns "bundle importer + validation report, artifact loading" and its criterion is "bundle imports clean", closed against a fixture in which none of the real artifact's awkward shapes occur. The shipped `BUNDLE.json` carries 42 files with bytes and sha256, 68 exporter self-checks and `total_bytes` 1,042,461,726; the importer reads three keys and none of the hashes, so a zeroed `equating_map.json` validates clean and a truncated `reviews.sqlite` validates and then dies inside the import transaction as a 500 with the staged tree left behind and no line naming the table. `_unpack` creates its directory before it opens the archive, never cleans up, and reuses whatever it finds, so the corpus tar cut at 90% validated `ok` over a 332-of-436 MB `reviews.sqlite`; the `DATA_DIR` boundary is a string prefix, under which `/database` passes for `/data`; referential integrity is never validated, so nine orphan variants validate clean and raise inside the transaction; the vocabulary version is derived four ways that disagree, and the refusal decision 163 exists for is skipped by exactly the bundle kind decision 162 says will arrive; and a models-only re-import loads none of the four curated ledgers it carries, while the validator reads two of them off that same bundle and discards them. M4.14 is that repair, and decisions **247-266** and **287** record the calls it needed — the lane's own range, spent across the opening sitting and three review cycles, and the one number past M4.15's block that a fourth cycle needed after it. It runs before M5 because decision 162 makes the first content import the only one this household will ever get — a corrupted seed that passes validation can be undone only by dropping the database — and because M5's flywheel and every later model update arrive through this door.

**M5.1 is the first of the seven that M5 ships as, and its row exists because this table gave that work one line (decision 321).** M5's Contents name four subsystems and its criterion could be closed while three of them were absent — "unattended" means nobody opens the admin view, so a build satisfying the sentence ships with no flywheel, no connector UI, one provider, one pass and the validator's retry path never executed. Decision 331 amends that criterion above rather than leaving the gap unstated, and decision 321 splits the work into M5.1 through M5.7, each of which writes its own row here when it opens. M5.1 is the part with no product of its own. §8's own preamble calls the raw store, the durable `(kind,key)` queue and the per-host rate-limited HTTP layer **ported skeleton**, and this app carried none of the three when the milestone opened: nothing in `backend/` named `raw_document`, `job_run` was an outcome log with no `(kind,key)` identity, no attempts, no lease and no scheduled retry, and the only outbound HTTP in the tree was a bare client per call with no rate limit, no backoff and no breaker. Two stages of §8 are meanwhile already built and have never had a caller: `placement/reconcile.py`'s `app_acquired` scope appears nowhere outside its own file, and `home/shelves.py`'s "New in the library" shelf has no producer, because nothing in the tree stamps `title.origin = 'acquired'` — the id partition §4.1 mints above 1e9 exists for exactly that write path and names it. This milestone is the machine between them. It runs first and alone because every other M5 lane plugs into the seams it publishes — the stage contract, the queue, the raw store — and because it holds `current_milestone`, the structural half of `backend/tests/spec_coverage.toml` and this table, which no second milestone may hold at the same time. Decisions 321, 322, 323, 331, 332, 336, 340 and 345 record the calls it needed, and its one migration is 0024_acquisition.sql.

**M5.3 is the port §8's own preamble warns about, and its row exists for decision 331's reason one milestone on.** §12 gave M5 one line; §8 gives stages 2, 3 and 4 four sentences and calls the corpus project's raw store, queue and HTTP layer **ported skeleton** while saying nothing about the part that is not skeleton. That part is this row. The corpus's `rebuild`, `project` and `ingest` are deliberately WHOLESALE — they re-derive the whole library — and §8's unit is one title, so the per-title incremental derive is new code by §8's own admission and is §14 risk 5's entire content: "port the invariants (raw/derived split, idempotent re-ingest, adjudication- and corrections-at-derivation — two distinct ledgers: DNA verdicts at ingest, source-credit facts at rebuild) or inherit the bugs they were built to kill (the 787-rows-reverted-twice scar)". Both curated ledgers are loaded by the importer today and read by nobody: a derive that regenerates rows without re-applying them reverts curated fixes silently, which is the one failure this milestone's exit criterion is written over. It runs in parallel with M5.2, M5.4 and M5.5 and shares no file with them; it does not hold this table or `current_milestone`, and under decision 346 it does not apply decision 171's owed §4.3 and §10 amendment either — that text is recorded in the register and applied by the milestone that holds this file. Decisions 326, 334, 335 and 372-378 record the calls it needed, and its one migration is `0026_acquisition_sources.sql`.

## 13. Evaluation

Surviving v1.1 §8 rows, inlined with their targets: pick-through > 60%; post-watch quality ≥ manual picking; rating capture > 70% of finished playbacks; winner approval share (target: the winner appears in a majority of every participant's favourable votes); satisfaction spread < 0.3; diversity drift — no sustained narrowing. Dropped rows: profile coverage over n_u, ledger drift over deficit_i, session length ≤ 10 (all tied to the deleted 8-axis/fairness machinery; the round's length is adaptive and capped at 20 by design, §6.2 step 4). Added: per-user held-out Spearman vs the crowd-derived expectation curves (§0 row 1's numbers are the M2 yardstick); within-liked resolution as duels accrue; not-seen rate in the rating queue (>50% = queue bug); cold-title badge accuracy once new titles get rated; DNA coverage of naming events (queries answered vs sent to flywheel); **shortlist stability** — how often Tonight's held-out pairs agree with the adaptive shortlist; and the **rate at which the round's cap and escape fire** (`session_participant.ended_by`, §14 risk 6).

Two mandated data streams from day one: (a) the 10% uniform-random comparison stream is the *only* data used to evaluate the tier model — adaptively-selected pairs inflate reliability (measured effect; the guard is non-negotiable), **and since §6.2's round became adaptive with a data-dependent stopping rule the same guard binds Tonight**: `session_answer.selection = 'uniform_holdout'` is drawn as a rate from a key sealed with the pool, is used for neither selection nor stopping, and is the only data admissible for evaluating the round (decision 223); (b) a separate silent **re-ask stream** — ~10% of comparisons/verdicts re-asked after ≥3 days; ~200 re-asks measure the flip rate σ that sets the tier budget (DNA_MODEL §4.2 build-order #1) and settles the corpus's zero-test-retest-data unknown. The two streams are different instruments: the uniform-random stream cannot measure test–retest consistency.

## 14. Risks

1. **Two-person reality vs crowd proxies** — every number is a crowd measurement; M2 is the test. Mitigation: expectations instrumented, not assumed.
2. **New-release DNA thinness** (craft-blurb gap, review-accrual lag) — mitigations in §8 stage 4/5; measure facet coverage of post-2025 titles.
3. **Jellyfin credential custody** — API keys are unscoped and admin-equivalent (no read-only variant exists), so the stored connector secret can administer the whole media server; write restraint is app-side policy only. This raises the stakes on §2's secrets encryption, and is why §7.3 prefers per-user tokens for the write path.
4. **WebAuthn origin coupling** — `PUBLIC_URL` change invalidates passkeys; wizard warns loudly.
5. **Per-title incremental derive is new code** replacing deliberately-wholesale corpus steps — port the invariants (raw/derived split, idempotent re-ingest, adjudication- **and corrections-**at-derivation — two distinct ledgers: DNA verdicts at ingest, source-credit facts at rebuild) or inherit the bugs they were built to kill (the 787-rows-reverted-twice scar).
6. **The vote round is unmeasured** — the adaptive round of §6.2 step 4 (v2.1's owner decision as rewritten by the 54a–54h fold) replaces the corpus-measured shortlist + question instrument. The surviving measured constraints are encoded in §6.2. **The instrument now exists and the comparison has not been made:** M4.12 made every answer loggable with its real latency, its `selection` arm and each seat's `ended_by`, so the rate at which the cap and the escape fire is readable (§4.2, §13) — but winner satisfaction has still never been compared against the solo baseline on a household's own evenings, and nobody tunes `boundary_z`, the cap or the escape until it has.
7. **Vocabulary evolution across the project boundary** — a vocabulary change is a **migration, not an import**. Under decision 162 a re-import carries models, not content, so a models-only bundle shipping `dna_vocab/v2` would leave `dna_tag` and `dna_projected` at v1 while the feature builder filters on the active version, and **both DNA blocks would go empty for every title** — a silent catastrophe rather than an error, and unrecoverable for the extracted tier, whose 2,016 quote-verified titles no re-projection reproduces. Until a migration plan exists the importer **refuses** a vocabulary version change and says why (decision 163), so no import this build performs can introduce a second one. Two vocabularies in the tables is a state this build does not create and must still survive — the way in is the migration decision 163 defers, which §8.4's naming-failure queue is what will eventually ask for — so **every read is scoped to one version**: a tilt, a conflict explanation or a DNA card computed over a superseded vocabulary is the app reasoning in a basis that is not the active row's (v1.1 §10.3-5 stands).

---

## Appendix A — requirement coverage

| Owner requirement | Where |
|---|---|
| Standalone app: backend, db, frontend | §1, §2 |
| Dockerized | §2 |
| Jellyfin connector via API + key | §7.1 |
| Admin UI for connectors (Jellyfin, LLM) | §6.6 |
| Extract all relevant data from this project into the new DB | §10 (+ corpus-side `mdc export-bundle`) |
| Data workflow for new entries, triggered on Jellyfin add | §7.2, §8 |
| Later HA integration, everything independent | §11 |
| Desktop + iPhone + Android browser UI | §6 preamble (PWA, phone-first) |
| Views: Home/Library, Rate, Tonight, Rank, Map, Taste, Admin | §6.0–6.6 |
| User management + biometric login | §6.6 Users (management, decision 166) · §3.2 (passkeys) · §12 M4.6 |
| Unseen marking in rating & pairwise UIs (owner 2026-08-29: one `not seen` control — "don't remember" is plain unseen) | §6.1, §4.2 |
| Unseen state synced with Jellyfin; account linking | §7.3, §3.3 |
| Tonight elicitation (owner 2026-08-29: candidate votes supersede the seen-only mood pairs; the count became adaptive in the 54a–54h fold) | §6.2 steps 4–6 |
| Participant flow: count prompt, push to named users' phones, guests on initiator's phone | §6.2 steps 1–2 |
| Taste comparison visualization (Patrick vs Jenny) | §6.5 |
| Rank view: tier table F–A+–S, filters, drag-drop that updates the model | §6.3, §5.2 |
| Comparisons increase resolution; relative 0–1 weights; rankings stay current | §5.2 (measured), σ-inflation refresh |
| CPU-only model updates | §1 constraint, §5.3 budgets |
| Explore view (connections, wandering) | §6.4 |
