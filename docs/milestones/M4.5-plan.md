# M4.5 — the seed: make the real bundle importable, and settle who owns the ids

**This milestone is not in §12.** It exists because §12's M0 importer was verified against a
fixture that does not resemble the artifact it stands in for, and because M5's exit criterion
("a new Jellyfin add reaches `ready` unattended") runs through §8 stage 9 — the placement path
that has never met the real feature contract. Branch `m45`, off `main` at `43fab6c`.

Owner decision, 2026-09-01: **seed milestone first, then M5.**

Sections read in full: §4.1, §4.3, §5.1, §5.3, §8, §10, §12, §14. The M5 planning work
(`docs/milestones/M5-plan.md`, the thirteen added M5 coverage rows) lives on branch `m5` and
rebases onto `main` after this merges.

---

## Why this milestone exists

Three agents investigated the owner's architecture change — a corpus survey, a Spielplan
coupling map, and a skeptic told to refute what the first two suggested. **Every finding below
was re-verified by hand** against the real bundle at
`movie_data_curator/data/export_bundle/v20260828/` before being written down.

### 1. The id design I was about to recommend is arithmetically wrong

The proposal was: adopt the corpus ids at seed time, then seed a sequence above the imported
maximum and mint from there. It rests on "the corpus never ships content again."

```
content.sqlite  sqlite_sequence:  title 21442   person 114503
                title max/count:  21442 / 19071
```

The corpus's next minted title id is **21443**. "Above the imported maximum" starts Spielplan
at **21443**. And the model bundle still carries content ids — `backbone.npz.title_id`,
`review_text_emb.npz.title_ids`, `seed_list.json`, `corrections_v1.tsv` are all keyed by corpus
`title.id` and all travel in `artifacts/`. One newly crawled film, and the next model bundle
asserts things about id 21443 while Spielplan's 21443 is a title the household acquired from
Jellyfin. `Backbone.row_of` resolves it, `classify_warm` stamps it warm, §5.1 scores it against
another film's embedding — and nothing trips, because `backbone.py:164` only checks that the
ids ascend, which they still do.

**What survives: partition the namespace, and assert the boundary.** Corpus ids stay verbatim,
because four read paths are id-keyed and the two artifacts with no id vector — `content_X.npz`
and `item_priors.npz` — cannot survive a permutation they have no way to check. Spielplan mints
from a **disjoint** range, so collision is impossible by construction rather than contingent on
the corpus standing still, and the importer refuses a bundle that reaches into Spielplan's
range. Range partitioning still cannot see the corpus *merging* two titles — which changes what
an id means without changing the id — so the model bundle also carries an identity column
row-aligned to `title_id`, and the importer checks it.

### 2. Three live defects on `main`, none caused by the architecture change

**The Cold Tower's credit block has been silently empty in every placement ever computed.**
`placement/features.py:253` keys it on `person_id::text`; the shipped `feature_contract.json`
names those 244 columns `p:director:Adam Arkin`, `p:composer:A.R. Rahman` — by name. Every key
misses, and the miss is invisible by construction: `features.py:96` marks the block `present`
because the query returned rows, counts the misses into `BuiltVector.unmapped`, and
`reconcile.py`'s `_UPSERT` never persists `unmapped`. Nothing is `dropped`, `is_thin` stays
False, no acquisition job parks. Cast and director contribute nothing to any coordinate.

**The real bundle cannot be imported at all.** `seed_list` is a total name collision — the
corpus's is a 238-row *list registry* (`id, slug, name, source, kind, category, weight,
item_count, fetched_at, notes`), Spielplan's is `position, title_id, decade`, and
`load.py:157` maps one onto the other, which COPYs 238 all-NULL rows into a NOT NULL primary
key. `corrections_v1.tsv`'s real header is `kind, title_id, value, evidence, note` while
`dna.py:232` reads `r["field"]` — an uncaught `KeyError`, not a `report.fail`.
`feature_contract.json`'s real top level is `content_blocks` plus a flat 6,435-entry
`feature_names` list; the fixture writes `blocks` plus a dict.

**Three shipped tables are loaded by nothing, and the report cannot say so.** `title_meta` —
named in §4.1's own table list, tabled at `0003_content.sql:72`, **46,318 rows** — appears
nowhere under `backend/spielplan/`. `title_list_membership` (35,295) and `imdb_ratings`
(14,858) likewise. They vanish without a report line, because `ImportReport` tracks unmapped
*columns within mapped tables*; an unmapped **table** is invisible.

### 3. How all of it survived, in one line of one test

`backend/tests/test_placement.py:578`:

```python
assert vec[contract.block("credit").column("3")] == 1.0
```

The fixture's contract declares the credit column as `"3"` — the person id the builder emits.
The test is named *"the vector is built from the database through the contract's own columns"*,
and the contract is the fixture's, whose columns were invented to match the code. Line 594 then
asserts `set(built.unmapped) == {"meta"}` — that credit has **no** unmapped keys — which against
the shipped contract would be 244 misses out of 244.

This is M4's lesson one level up. `M4-open-points.md` §5 names five patterns for a test that
cannot fail; the fifth is *a test that scores the implementation with the same function the
implementation optimises*. A fixture written from the same reading as the importer is that
same failure at the level of the artifact: it tests the reading, not the world.

`README.md` says *"The corpus bundle does not exist in this repo, so the importer is verified
against a synthetic fixture."* It does exist, built 2026-08-28. That sentence is the root cause
and it gets corrected here.

---

## Opening the milestone

`M4.5` inserted into `test_spec_coverage.py`'s `MILESTONES` between M4 and M5, with a comment
saying why a milestone exists that §12 does not have. `current_milestone = "M4.5"`:

```
  > M0   33/35  covered (1 waived)
  > M4   42/42  covered
  > M4.5   0/12  covered
```

Thirteen rows owed — twelve M4.5 rows plus one M0 row whose waiver this milestone discharges.
All written before any code. Grouped by kind, in build order:

### backend (pytest, no database) — 4 rows

| row | asserts | touches |
|---|---|---|
| `data-rules-fixture-matches-the-shipped-artifact` | a committed shape manifest, derived from a real bundle, pins the shapes the importer relies on; the fixture is checked against it, and the real bundle too when `CORPUS_BUNDLE_DIR` is set | new `tests/fixtures/real_bundle_shapes.json`, `make_bundle.py` |
| `data-rules-credit-block-keyed-as-the-contract-names-it` | the credit block uses the contract's own `p:<role>:<name>` columns; a title whose crew the contract names produces a non-zero block | `placement/features.py` |
| `data-rules-a-feature-block-that-never-hits-is-not-present` | present / legitimately-absent / all-keys-miss are three distinguishable states; the miss count is persisted and `is_thin` sees it | `placement/features.py`, `placement/reconcile.py`, migration |
| `data-rules-corrections-ledger-parsed-at-its-real-header` | the real header parses; an unrecognised one fails the report naming the column, never raises | `importer/dna.py` |

### integration (pytest against Postgres) — 8 rows, plus the discharged M0 row

| row | asserts | touches |
|---|---|---|
| `data-rules-import-reports-every-shipped-table` | every shipped table is loaded with a count or named as skipped with a reason; silence is impossible | `importer/load.py`, `importer/report.py` |
| `data-rules-title-meta-multi-source-rows-kept` | `title_meta` loads with its per-source rows kept, and is readable | `importer/load.py`, `db/library.py` |
| `data-rules-seed-registry-and-onboarding-list-are-distinct` | the registry and the onboarding list are different tables; the registry writes nothing into the onboarding list | `importer/load.py`, migration |
| `platform-app-minted-ids-are-partitioned-from-the-corpus` | disjoint mint range; the importer refuses a bundle reaching into it, naming the id; the sequence is positioned by the seed import, not the migration | migration, `importer/load.py`, `importer/validate.py` |
| `platform-model-bundle-identity-is-checked-not-trusted` | an identity column row-aligned to `title_id`, checked against `title`; absent identity is itself a failure | `importer/validate.py`, `scoring/backbone.py` |
| `platform-content-seeds-once-models-reimport` | a models-only bundle imports and swaps; a second content seed is refused; the two kinds are distinguishable in `artifact_bundle` | `importer/bundle.py`, migration |
| `platform-movie-data-backup-and-restore` | content + DNA + reviews and **nothing user-specific**; restore carries the sequence positions and preserves `title.origin` | new `spielplan/backup/`, `api/admin.py`, `worker.py` |
| `platform-backup-restore-ordering-is-explicit` | a model bundle loaded into an install with no content is refused *before* writing, naming the prerequisite | `importer/bundle.py` |
| `platform-backup-rotation-and-ciphertext` **(M0, waiver discharged)** | §2's nightly `pg_dump` to `/data/backups`, rotation 14, ciphertext-only, undecryptable without `SECRETS_KEY` | `worker.py`, `ops/` |

### Schema

`backend/migrations/0015_seed.sql`, a new numbered file — **no applied migration is edited**.
`title_id_seq` / `person_id_seq` with the partitioned start; the seed-list rename;
`title_placement` gains the persisted miss counts; `artifact_bundle` gains the seed/model kind.
The one pre-existing FK defect found on the way — `user_vector.bundle_version` is nullable but
`ON DELETE CASCADE`, so deleting a bundle row deletes the whole fold-in vector rather than
nulling the stamp, where the sibling `ledger_fit.bundle_version` correctly uses `SET NULL` — is
fixed here too, because this milestone is where bundle rows first become prunable.

---

## What I am not deciding alone

Both were asked and both are answered.

**The corpus repo is now under version control.** It was not a git repository — `git log`
exited 128 — and the seed/model exporter split touches `mdc/export_bundle.py`, so the owner's
call was to baseline it first. Done: `2a0df7b`, 670 files, 11 MB, ignoring `data/` (the 1 GB
export bundle and the crawl store), `.venv/`, `.env` and `scratchpad/`'s 37 MB of run logs.
Every exporter edit now has an undo and a reviewable diff. The seam it needs already exists —
`_export_model_artifacts` (`mdc/export_bundle.py:292`) and `_copy_tables`
(`mdc/export_bundle.py:193`) are already separate functions — so the split is a flag, not a
rewrite.

**Backup/restore is in this milestone.** Two rows, and one consequence worth stating: it
discharges the standing M0 waiver on `platform-backup-rotation-and-ciphertext`, whose reason
was literally "the nightly dump job does not exist yet". That waiver is removed rather than
narrowed, so the M0 row now owes a real test here — §2's nightly `pg_dump` to `/data/backups`,
rotation 14, ciphertext-only, and undecryptable without `SECRETS_KEY`. Its second half needs a
`pg_dump` binary the backend image does not carry (`ops/backend.Dockerfile` installs `libpq5`
and `curl` only), while the `db` service is `postgres:16` and already mounts `/backups` — a
Phase 2 decision to be recorded when it is made, not a reason to keep the waiver.

**M4.5 therefore removes one waiver and adds none.**

---

## Decisions made without you

*(Populated during Phase 2.)*

Nothing yet — no implementation code has been written.
